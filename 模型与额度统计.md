# 模型与额度统计

> 更新：2026-08-12（用户确认额度；三个短板改造前基线）
> 来源：阿里云百炼控制台 + 智谱 z.ai 控制台 + 本地 model_configs 表
> ⚠️ 重要：**配音/质检/生成一律优先阿里云百炼**；智谱与火山引擎用户声明已无法使用（GLM 文本额度除外，见下表）。

---

## 一、语音合成（TTS）— 阿里云百炼 CosyVoice ✅ 已开通

> 三个短板之「配音」的主模型族。全部「免费额度用完即停」，各模型独立 10,000 次额度。

| 模型 ID | 剩余/总 | 免费额度到期 | 状态 | 备注 |
|---------|---------|-------------|------|------|
| `cosyvoice-v2` | 10,000 / 10,000 | 2026-09-07 | 已开启 | 经典中文音色，**首选（男声 longcheng_v2 / 女声 longxiaochun_v2）** |
| `cosyvoice-v3.5-plus` | 10,000 / 10,000 | 2026-09-07 | 已开启 | ⚠️ 无系统音色（仅声音复刻/设计），**不入 HTTP 降级链** |
| `cosyvoice-v3.5-flash` | 10,000 / 10,000 | （用户未提供，按 09-07 记录） | 已开启 | ⚠️ 同上，无系统音色，不入降级链 |
| `cosyvoice-v3-flash` | 10,000 / 10,000 | 2026-09-07 | 已开启 | 快速版（男声 longcheng_v3 / 女声 longxiaochun_v3），降级链第 2 位 |
| `cosyvoice-v3-plus` | 10,000 / 10,000 | 2026-09-07 | 已开启 | 高音质（男声 longanyang / 女声 longanhuan），降级链第 3 位 |
| `cosyvoice-v1` | 10,000 / 10,000 | **2099-01-01** | 已开启 | ⚠️ **不支持 HTTP 调用**（400「current user api does not support http call」，仅 WebSocket），不入降级链 |
| `cosyvoice-clone-v1` | 10,000 / 10,000 | **2099-01-01** | 已开启 | 音色克隆（二期分角色用） |

**实测（2026-08-12 本地全链路验证）**：v2/v3-flash/v3-plus 三模型全部打通（同步 SpeechSynthesizer 接口，2~3s 返回，mp3 正常 ffprobe）。**当前降级链 = `cosyvoice-v2 → cosyvoice-v3-flash → cosyvoice-v3-plus`**，OpenAI TTS 兜底。

**规划**：第一版统一男旁白 → `cosyvoice-v2`（稳定兜底 `cosyvoice-v1`）；二期分角色多音色 → 音色 ID 映射 + 可选 `clone-v1` 克隆。

---

## 二、文本大模型（LLM）— 智谱 GLM（备用）+ 阿里云 qwen（在用）

> 项目 `llm_provider=auto`，主链通义 qwen-plus（剧本/扩写/规划，额度大且稳定）；智谱 GLM 额度仍在，保留为备用链。

| 模型 ID | 剩余/总 | 免费额度到期 | 状态 | 备注 |
|---------|---------|-------------|------|------|
| `glm-4.5-air` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 | 智谱轻量 |
| `glm-4.5` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 | |
| `glm-4.6` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 | |
| `glm-4.7` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 | |
| `glm-5` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 | |
| `glm-5.1` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 | |
| `glm-5.2` | 1,000,000 / 1,000,000 | **2026-09-15** | 已开启 | 最晚到期 |

（阿里云 qwen-plus 等文本额度用户未提供具体数值，长期在用、成本可忽略，暂不列表。）

---

## 三、视觉模型（质检/智能描述）— 阿里云 qwen-vl ✅ 已在用

> 三个短板之「质检」的模型族。**已实证可用**（2026-08-05 本地实测：720p 单张 2042 image tokens）。

| 模型 ID | 用途 | 状态 | 备注 |
|---------|------|------|------|
| `qwen3-vl-flash` | 质检/模板分析/智能描述（首选） | ✅ 实测可用 | 便宜快，8 帧 ≈16k tokens |
| `qwen3.5-omni-plus` | 兜底 | ✅ 可用 | 原首选，tokens 贵（42k/次）已降级 |

（智谱 glm-4v 视觉兜底代码仍在 ai-service.util.ts，但**智谱已声明不可用 → 质检只用阿里云**。）

---

## 四、视频生成（wan 系列）— 阿里云 ✅ 在用

> 次数额度（非 token）。以下为本地 model_configs 活跃项（2026-08-11 查询）+ AI-MODEL-STATUS 记录的次数额度。

| 模型 ID | 能力 | 优先级 | 次数额度（历史记录） |
|---------|------|--------|---------------------|
| `wan2.6-r2v-flash` | R2V | 1 | 参考图视频主链 |
| `wan2.7-i2v-2026-04-25` | I2V | 3 | 各 50 次 |
| `wan2.5-i2v-preview` | I2V | 4 | 各 50 次 |
| `wan2.2-i2v-plus` | I2V | 5 | 各 50 次 |
| `wanx2.1-i2v-plus` | I2V | 6 | 各 50 次 |
| `wan2.7-i2v` | I2V | 8 | 各 50 次 |
| `wan2.7-t2v-2026-06-12` | T2V | 9 | 50 次 |
| `wanx2.1-t2v-turbo` | T2V | 11 | 175 次 |
| `wanx2.1-t2v-plus` | T2V | 12 | 180 次 |

> ⚠️ 用户未提供最新视频次数额度 → 三个短板之「多候选择优」的 N 取值依据此表历史额度，默认 N=1，关键镜头 N=2~3（60~90 个关键镜头预算）。

---

## 五、文生图（wanx 系列）— 阿里云 ✅ 在用

| 模型 ID | 优先级 | 备注 |
|---------|--------|------|
| `wanx2.1-t2i-plus` | 1 | 主链 |
| `wan2.6-t2i` | 2 | |
| `wanx2.1-t2i-turbo` | 3 | 快版 |
| `wan2.5-t2i-preview` / `wan2.2-t2i-plus` / `wan2.2-t2i-flash` / `wanx2.0-t2i-turbo` / `wanx-v1` | 4~8 | 降级链 |

---

## 六、使用规划（三短板落地）

| 能力 | 模型 | 依据 |
|------|------|------|
| 配音 | `cosyvoice-v2`（降级 v3-flash → v3-plus；v1/v3.5 不可用） | 10,000 次免费 |
| BGM | 在线免版权曲库直链（URL 元数据，临时下载用完即删） | 磁盘紧张，零占用 |
| 质检 | `qwen3-vl-flash`（抽 3~5 帧/镜头） | 实测 2042 tokens/张，免费额度内 |
| LLM 规划 | qwen-plus（主）+ GLM 备用 | 已在用 |
| 视频候选 | 默认 N=1，关键镜头 2~3 | 次数额度有限 |

## 七、风险备注

- CosyVoice 各模型免费额度 2026-09-07 到期（v1/clone-v1 至 2099）→ 到期前评估购买或切长期模型
- 智谱/火山不可用 → 任何降级链不得回落到智谱/火山（代码中若有备用链需在改造时移除或标注）
- 视频次数额度为历史记录，未含最新消耗 → 多候选功能上线前建议在百炼控制台复核一次
