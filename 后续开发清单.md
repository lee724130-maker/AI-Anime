# AI 视频项目后续开发清单

> 目标：把当前项目从“单点 AI 视频生成工具”升级为“工作台 + AI 生成中心 + 短剧工作室”的完整创作平台。本文先记录产品流程、功能边界、开发阶段、数据结构和需要你补充的资源，确认后再进入代码开发。

## 0. 复核结论

当前清单的主流程是正确的，可以作为后续开发总蓝图使用。为了避免第一步开发后返工，正式开工前需要明确一个原则：

- 第一版先跑通完整闭环，不追求一次性做完所有高级能力。
- 优先保证“项目、任务、媒体文件、模型能力、提示词、生成记录”这些底层结构稳定。
- 所有批量生成能力都必须有草稿保存、成本预估、失败重试和人工替换入口。
- 多图合并、配音、字幕、背景音乐、真实支付、对象存储可以预留结构，但不阻塞第一版闭环。

因此第一步不应该只做前端导航，还要同步补齐最小数据底座。推荐第一步开发范围是：

```text
产品导航 + 工作台骨架 + 短剧项目基础表 + 统一媒体文件表 + 统一任务状态规范
```

这样后面的 AI 生成中心、资产库、分集视频都能接在同一套项目和任务体系上。

## 1. 当前理解

你想要的不是只新增几个按钮，而是搭建一套完整的 AI 视频生产流程：

- 第一栏：工作台，统一查看项目、作品、任务、资产、最近生成记录和常用入口。
- 第二栏：AI 生成视频和图片，支持文字生视频、图片生视频、文字生图片、多图合并，并且每种生成都可以填写描述和提示词增强效果。
- 第三栏：短剧工作室，从剧本大纲开始，自动分析短剧结构，生成角色/物品/场景资产，再按分集和片段批量生成视频，最后合成为每一集。

当前项目已经有前端、后台、用户、脚本、角色、视频任务、队列、模型配置、视频拼接等基础能力，但产品流程还比较分散，短剧生产流程也没有形成完整的“项目化流水线”。

## 2. 推荐整体导航结构

建议用户端主导航改为三大区：

| 导航 | 页面 | 主要作用 |
| --- | --- | --- |
| 工作台 | `/dashboard` | 查看所有项目、最近任务、生成记录、资产统计、算力余额、失败任务提醒 |
| AI 生成 | `/generate` | 普通图片/视频生成中心，面向单次或批量创作 |
| 短剧工作室 | `/drama` | 面向短剧项目，从大纲到资产到分集视频的一体化流程 |

保留后台管理端，用于用户、模型、Key、系统配置、日志、订单、算力、额度等运维功能。

## 3. 工作台流程

### 3.1 工作台要展示的内容

- 所有项目：短剧项目、普通生成项目、未完成草稿。
- 最近生成任务：图片、视频、片段、合成视频。
- 任务状态：排队中、生成中、成功、失败、可重试。
- 资产统计：人物图、物品图、场景图、视频片段、成片。
- 算力余额和预计消耗入口。
- 常用功能入口：新建短剧、文字生视频、图片生视频、文字生图片、多图合并。

### 3.2 工作台需要补齐的能力

- 项目概念：当前视频、脚本、角色是分散资源，需要新增“项目/短剧项目”的统一归属。
- 最近活动：记录用户最近编辑、生成、失败、合成的行为。
- 任务总览：统一图片任务、视频任务、短剧片段任务、合成任务。
- 快捷继续：用户可以从未完成步骤继续，例如继续编辑资产库或继续生成某一集。

## 4. AI 生成中心

### 4.1 功能分区

AI 生成中心建议使用同一个页面，通过模式切换展示不同表单：

| 模式 | 输入 | 输出 | 必备参数 |
| --- | --- | --- | --- |
| 文字生视频 | 文字描述、提示词增强 | 视频 | 模型、比例、清晰度、时长、风格 |
| 图片生视频 | 图片、动作/镜头描述、提示词增强 | 视频 | 模型、比例、清晰度、时长、首帧/参考图 |
| 文字生图片 | 文字描述、提示词增强 | 图片 | 模型、比例、清晰度/尺寸、生成数量、风格 |
| 多图合并 | 多张图片、合并描述、主体关系说明 | 图片或视频 | 模型、比例、输出类型、清晰度 |

### 4.2 通用生成参数

- 模型：按能力分类展示，例如图片模型、视频模型、图片转视频模型、视频编辑模型。
- 比例：9:16、16:9、1:1、4:3、3:4、21:9。
- 清晰度：480p、720p、1080p，图片可换算为对应尺寸。
- 时长：5s、10s、15s，后续根据模型能力开放更长。
- 图片生成数量：1、2、4、8，受模型限制时自动禁用不支持数量。
- 风格：动漫、真人、电影感、国风、写实、赛博、可扩展。
- 提示词增强：用户原始描述 + 系统增强词 + 负面提示词。
- 参考素材：可选择已有资产，也可以本地上传。
- 生成用途：普通生成、短剧资产、短剧片段、封面、草稿预览，需要在任务中区分。
- 任务优先级：单次生成优先，批量生成进入队列，避免大量片段同时请求模型导致失败。

### 4.3 生成记录

每一次生成都需要保留：

- 原始描述。
- 最终发送给模型的提示词。
- 模型名称。
- 参数：比例、清晰度、时长、数量、风格。
- 输入素材。
- 输出文件。
- 消耗算力。
- 失败原因和重试入口。
- 任务来源：工作台、AI 生成中心、短剧资产库、短剧分集片段。
- 任务状态历史：记录创建、排队、开始、完成、失败、重试时间。

### 4.4 上传和媒体文件

AI 生成中心和短剧工作室都会依赖本地上传和生成文件，因此需要统一媒体文件管理：

- 上传图片：作为参考图、角色图、物品图、场景图、首帧图。
- 上传视频：作为替换片段、合成素材、历史作品。
- 生成图片：模型返回的角色图、场景图、物品图、普通图片。
- 生成视频：普通视频、短剧片段、合成成片。
- 封面图：视频封面、短剧项目封面、资产预览图。

媒体文件需要记录：

- 文件类型：image、video、audio、cover。
- 来源：upload、generated、stitch、extract-cover。
- 所属项目、所属资产、所属片段或所属任务。
- 本地路径或远程 URL。
- 文件大小、宽高、时长、格式。
- 是否可删除、是否主图、是否备用图。

## 5. 短剧工作室完整流程

短剧工作室建议拆成四个步骤：

1. 短剧首页。
2. 新建短剧/剧本大纲。
3. 资产库。
4. 分集视频。

### 5.1 短剧首页

页面目标：新建短剧和展示所有短剧。

需要展示：

- 短剧项目列表。
- 项目封面。
- 标题、题材、集数、每集时长、比例、清晰度。
- 当前进度：大纲待分析、资产待生成、片段生成中、可合成、已完成。
- 最近更新时间。
- 继续制作按钮。

### 5.2 新建短剧页面

用户输入：

- 短剧标题。
- 剧本大纲。
- 目标风格。
- 比例。
- 清晰度。
- 单片段时长或单集目标时长。
- 可选模型。

AI 分析后输出：

- 短剧题材。
- 建议集数。
- 每集标题。
- 每集剧情梗概。
- 每集预计时长。
- 主要人物。
- 重要物品。
- 主要场景。
- 分集片段列表。

用户确认后进入资产库。

补充要求：

- AI 分析结果必须先进入“可编辑确认页”，不能直接进入批量生成。
- 用户可以修改集数、分集标题、片段数量、片段提示词、资产描述。
- 用户确认后再生成资产，避免模型分析不准导致大量无效消耗。

### 5.3 剧本分析逻辑

模型需要把大纲解析成结构化数据，而不是只返回普通文本。

建议结构：

```json
{
  "title": "短剧标题",
  "genre": "题材",
  "episodeCount": 12,
  "targetAudience": "目标受众",
  "style": "画面风格",
  "episodes": [
    {
      "episodeNo": 1,
      "title": "第 1 集标题",
      "summary": "本集概述",
      "duration": 60,
      "segments": [
        {
          "segmentNo": 1,
          "summary": "片段内容",
          "prompt": "视频生成提示词",
          "characters": ["角色 A"],
          "props": ["物品 A"],
          "scenes": ["场景 A"],
          "duration": 5
        }
      ]
    }
  ],
  "assets": {
    "characters": [
      {
        "name": "角色名",
        "role": "角色定位",
        "description": "外貌、服装、性格、年龄",
        "prompt": "人物图生成提示词"
      }
    ],
    "props": [
      {
        "name": "物品名",
        "description": "用途和外观",
        "prompt": "物品图生成提示词"
      }
    ],
    "scenes": [
      {
        "name": "场景名",
        "description": "地点、氛围、光线、构图",
        "prompt": "场景图生成提示词"
      }
    ]
  }
}
```

后端必须校验这份 JSON：

- 缺字段时自动补默认值。
- 片段时长总和不能明显超过单集目标时长。
- 片段引用的人物、物品、场景必须能在资产列表里找到。
- JSON 解析失败时保留原始模型返回内容，给用户重新分析入口。

### 5.4 资产库

资产库来自剧本大纲分析结果，自动生成三类资产：

| 资产类型 | 示例 | 必备字段 |
| --- | --- | --- |
| 人物图 | 男主、女主、反派、配角 | 名称、角色定位、外貌描述、性格、服装、参考图、生成提示词 |
| 物品图 | 信物、武器、道具、核心物件 | 名称、用途、外观描述、所属剧情、生成提示词 |
| 场景图 | 校园、宫殿、街道、房间 | 名称、时代/地点、氛围、光线、构图、生成提示词 |

资产库能力：

- 一键生成全部资产。
- 单个资产重新生成。
- 修改描述后重新生成。
- 手动上传替换图片。
- 添加新角色、新物品、新场景。
- 标记主图/备用图。
- 记录生成模型、参数、提示词。
- 资产版本管理：同一资产可生成多张候选图，用户选择其中一张作为主图。
- 资产锁定：用户确认满意后可锁定，后续重新分析剧本不覆盖该资产。

### 5.5 分集视频

进入分集视频后，系统自动使用前面生成的人物图、物品图、场景图，并按剧本分析结果分好集数和片段。

每一集需要展示：

- 集数、标题、剧情概要。
- 当前生成状态。
- 片段数量。
- 已生成片段数量。
- 合成状态。
- 成片预览。

每个片段需要展示：

- 片段编号。
- 片段剧情。
- 绑定人物图。
- 绑定物品图。
- 绑定场景图。
- 视频生成提示词。
- 模型。
- 比例。
- 清晰度。
- 时长。
- 状态。
- 输出视频。
- 失败原因。
- 重新生成入口。

### 5.6 批量生成与合成

推荐流程：

1. 用户确认某一集的片段和素材绑定。
2. 点击“批量生成本集片段”。
3. 系统逐个或并发提交片段生成任务。
4. 片段生成完成后，用户可预览和替换失败片段。
5. 点击“合成本集视频”。
6. 系统按片段顺序拼接视频。
7. 生成本集成片，可下载、预览、继续生成下一集。

需要注意：

- 合成前必须检查所有片段是否已完成。
- 支持失败片段单独重试。
- 支持跳过某个片段或手动上传替换片段。
- 合成时统一分辨率、比例、帧率、音频策略。
- 后续可增加字幕、配音、背景音乐、片头片尾。
- 片段使用的素材快照要保存下来，避免资产库后续改图导致历史片段无法复现。
- 本集合成后仍允许替换某个片段并重新合成。

## 6. 建议新增数据模型

当前已有 `scripts`、`characters`、`video_tasks`。为了完整短剧流程，建议新增或扩展：

| 数据表 | 作用 |
| --- | --- |
| `projects` | 统一项目表，区分普通生成项目和短剧项目 |
| `drama_projects` | 短剧项目主表，保存标题、题材、集数、比例、清晰度、状态 |
| `drama_outlines` | 剧本大纲和 AI 分析结果 |
| `drama_assets` | 人物、物品、场景资产统一表 |
| `drama_episodes` | 分集信息 |
| `drama_segments` | 每集片段信息、提示词、素材绑定 |
| `generation_tasks` | 统一图片/视频/资产/片段/合成任务 |
| `media_files` | 统一存储生成图片、视频、封面、上传文件 |
| `model_configs` | 模型能力、参数范围、启用状态、价格 |
| `prompt_templates` | 系统提示词模板，区分剧本分析、资产生成、片段生成、提示词增强 |
| `task_events` | 任务状态历史，方便排查失败和展示进度 |

也可以先不大拆表，用现有表扩展 MVP，但长期建议至少先建立 `media_files`、`generation_tasks`、`drama_projects` 三个基础结构，否则后面会越来越难维护。

## 7. 后端接口规划

### 7.1 工作台

- `GET /api/workbench/summary`：项目数量、任务状态、资产统计、算力余额。
- `GET /api/workbench/recent`：最近项目和最近生成记录。

### 7.2 AI 生成中心

- `POST /api/generate/image/text`：文字生图片。
- `POST /api/generate/video/text`：文字生视频。
- `POST /api/generate/video/image`：图片生视频。
- `POST /api/generate/image/merge`：多图合并。
- `GET /api/generate/tasks`：生成任务列表。
- `GET /api/generate/tasks/:id`：生成任务详情。
- `POST /api/generate/tasks/:id/retry`：重试任务。
- `POST /api/media/upload`：上传图片或视频。
- `GET /api/media/:id`：媒体文件详情。
- `DELETE /api/media/:id`：删除未被锁定的媒体文件。

### 7.3 短剧工作室

- `GET /api/dramas`：短剧列表。
- `POST /api/dramas`：创建短剧。
- `GET /api/dramas/:id`：短剧详情。
- `POST /api/dramas/:id/analyze`：分析剧本大纲。
- `PUT /api/dramas/:id/analysis`：用户确认或修改分析结果。
- `GET /api/dramas/:id/assets`：资产库。
- `POST /api/dramas/:id/assets/generate-all`：批量生成资产。
- `POST /api/dramas/:id/assets`：新增资产。
- `PUT /api/dramas/:id/assets/:assetId`：修改资产。
- `POST /api/dramas/:id/assets/:assetId/generate`：单个资产生成/重生成。
- `GET /api/dramas/:id/episodes`：分集列表。
- `GET /api/dramas/:id/episodes/:episodeId`：单集详情。
- `POST /api/dramas/:id/episodes/:episodeId/generate-segments`：批量生成本集片段。
- `POST /api/dramas/:id/segments/:segmentId/generate`：单片段生成。
- `PUT /api/dramas/:id/segments/:segmentId`：修改片段提示词、素材、参数。
- `POST /api/dramas/:id/episodes/:episodeId/stitch`：合成本集视频。

## 8. 模型与插件需求

### 8.1 必须具备的模型能力

| 能力 | 用途 | 需要你提供或确认 |
| --- | --- | --- |
| 大语言模型 | 分析剧本、拆分集数、生成资产描述、生成片段提示词 | OpenAI、DeepSeek、通义千问、豆包等任选一种稳定可用 |
| 文字生图片 | 生成人物图、物品图、场景图、普通图片 | Seedream、通义万相、OpenAI 图片模型等 |
| 图片生视频 | 用人物/场景参考图生成片段视频 | Seedance、通义万相 I2V、Runway 等 |
| 文字生视频 | 普通 AI 生成中心、无参考图片段 | Seedance、通义万相 T2V 等 |
| 多图合并 | 多参考图融合成图或作为视频参考 | 需要确认选用模型是否原生支持多图输入 |
| 视频拼接 | 片段合成为一集 | 当前可继续使用 FFmpeg |

### 8.2 模型能力表

需要在系统里维护模型能力，而不是把模型写死在页面里：

| 字段 | 说明 |
| --- | --- |
| 模型名称 | 页面展示名 |
| 模型 ID | 实际调用 API 的 ID |
| 供应商 | 火山、阿里、OpenAI、DeepSeek、Runway 等 |
| 能力 | T2I、T2V、I2V、多图、视频编辑、LLM |
| 支持比例 | 9:16、16:9、1:1 等 |
| 支持清晰度 | 480p、720p、1080p 等 |
| 支持时长 | 5s、10s、15s 等 |
| 支持图片数量 | 1、2、4、8 |
| 是否启用 | 后台控制 |
| 单价/算力消耗 | 用于生成前预估 |

前端所有模型选项都应该从这个能力表读取，这样后续换模型时不用频繁改页面代码。

### 8.3 你需要提供或确认的信息

- 使用哪些模型供应商：火山引擎、阿里云、OpenAI、DeepSeek、Runway 等。
- 各供应商 API Key。
- 模型是否已开通：尤其是视频模型和多图合并模型。
- 希望默认使用动漫风格还是真人风格。
- 短剧默认比例：建议短视频优先 9:16。
- 短剧默认清晰度：建议先用 720p 控制成本，后续开放 1080p。
- 单片段默认时长：建议 5s，稳定后再支持 10s/15s。
- 每集默认片段数或每集目标时长。
- 是否需要配音、字幕、背景音乐、封面生成。
- 是否需要接入对象存储，例如阿里云 OSS、腾讯云 COS、Cloudflare R2。
- 是否需要真实支付，还是继续先用模拟支付。

## 9. 开发阶段建议

### 阶段一：产品骨架和导航

- 重构用户端主导航为工作台、AI 生成、短剧工作室。
- 建立短剧项目基础结构。
- 建立统一媒体文件结构。
- 建立统一任务状态规范。
- 工作台展示项目、任务、资产、算力和快捷入口，可以先使用已有数据和基础统计。
- 新建统一的短剧首页。
- 确定项目状态流转。

验收标准：

- 用户能清楚看到三个主功能区。
- 用户能从工作台进入普通生成和短剧制作。
- 短剧项目能创建、查看、继续编辑。
- 上传/生成文件有统一归属。
- 后续任务能挂到项目、媒体、资产或片段上。

### 阶段二：AI 生成中心

- 做统一生成页面。
- 支持文字生图片、文字生视频、图片生视频。
- 增加多图合并入口，若模型暂不支持，先做前端和任务结构预留。
- 保存提示词、参数、模型、生成结果。
- 统一生成历史和重试。
- 从模型能力表读取可用模型和参数范围。

验收标准：

- 用户可以用同一页面完成常规图片/视频生成。
- 每次生成记录可追踪、可复用、可重试。

### 阶段三：短剧大纲分析

- 新建短剧项目。
- 输入剧本大纲。
- 调用大语言模型输出结构化分析。
- 用户可编辑集数、题材、分集、片段、提示词。
- 保存分析结果。
- 保存原始大纲、模型原始返回、修正后的结构化结果。

验收标准：

- 输入一段大纲后，系统能生成集数、题材、人物、物品、场景、分集片段。
- 用户可以修改 AI 分析结果。

### 阶段四：资产库

- 根据分析结果生成资产列表。
- 支持人物、物品、场景三类资产。
- 支持批量生成资产图。
- 支持单个资产重新生成、上传替换、新增资产。
- 资产绑定到短剧项目。

验收标准：

- 每个资产都有名称、描述、提示词、图片、生成状态。
- 用户能补充和修正资产。

### 阶段五：分集视频

- 根据分析结果生成分集和片段。
- 自动绑定人物、物品、场景资产。
- 片段可修改提示词和参数。
- 支持批量生成片段。
- 支持单片段重试。
- 支持本集合成。

验收标准：

- 用户能完成“某一集”的片段生成和视频合成。
- 失败片段不会阻塞整个项目，能单独重试。

### 阶段六：体验和稳定性

- 进度条细化。
- 失败原因中文化。
- 任务队列并发控制。
- 成本预估和扣费规则完善。
- 文件存储清理和持久化。
- 生成结果预览优化。
- 移动端适配。
- 自动保存草稿。
- 批量任务暂停、继续、取消。

验收标准：

- 长任务可感知进度。
- 用户知道失败原因和下一步操作。
- 文件不会无限堆积。

## 10. 关键状态流转

### 10.1 短剧项目状态

```text
草稿
  -> 大纲待分析
  -> 大纲已分析
  -> 资产待生成
  -> 资产生成中
  -> 资产已完成
  -> 片段待生成
  -> 片段生成中
  -> 可合成
  -> 已合成
  -> 已完成
```

### 10.2 片段状态

```text
待生成 -> 排队中 -> 生成中 -> 已完成
                         -> 失败 -> 可重试
```

### 10.3 资产状态

```text
待生成 -> 生成中 -> 已完成
                 -> 失败 -> 可重试
```

## 11. 需要重点注意的问题

- 模型能力差异很大，前端参数要根据模型动态禁用不支持项。
- 多图合并需要确认具体模型是否支持，否则不能只做假入口。
- 短剧分析必须要求模型返回 JSON，并做后端校验和兜底修复。
- 视频片段合成前要统一比例、分辨率、编码格式。
- 角色一致性是短剧质量核心，需要保留角色参考图，并尽量走图片生视频。
- 资产和片段都要支持人工修改，不能完全依赖 AI 一次生成。
- 生成任务成本较高，批量生成前必须展示预计算力消耗。
- 长流程必须自动保存草稿，避免刷新丢失。
- 任务扣费建议采用“提交时冻结、成功后扣除、失败后退回或解冻”的策略，避免用户失败也被扣费。
- 真实模型返回的远程 URL 可能过期，需要尽快下载到本地或对象存储。

## 12. 我建议优先确认的产品决策

在正式开发前，建议你先确认这些问题：

- 是否把“短剧”作为最高优先级，普通 AI 生成中心作为第二优先级？
- 短剧第一版是否只做“一集完整生成”，跑通后再扩展多集批量？
- 是否第一版先不做配音、字幕、音乐，只做画面片段和视频合成？
- 是否默认 9:16、720p、5s 片段，降低成本和失败率？
- 是否继续使用当前火山/阿里/OpenAI/DeepSeek 的模型配置方式？
- 是否需要我后续直接重构前端导航和页面，还是先只补短剧工作室？
- 模型生成失败时是否允许自动切换备用模型？
- 生成内容是否需要审核或敏感词过滤？

## 13. 第一版 MVP 建议范围

为了最快把完整流程跑通，第一版建议这样做：

- 工作台：展示项目和任务总览。
- AI 生成中心：先完成文字生图片、文字生视频、图片生视频。
- 短剧工作室：完成短剧首页、新建短剧、大纲分析、资产库、单集片段生成、单集合成。
- 底层能力：统一媒体文件、统一任务状态、模型能力表、提示词模板。
- 暂缓：多集全自动批量、配音、字幕、音乐、复杂多图合并、发布到平台、真实支付。

这样可以先验证核心链路：

```text
剧本大纲 -> AI 分析 -> 资产生成 -> 分集片段 -> 批量生成 -> 合成本集
```

确认这个链路稳定后，再扩展成完整的多集短剧生产系统。

## 14. 分阶段施工清单

> 这一部分作为正式开发时的执行顺序。每个阶段都包含目标、具体任务、涉及页面、涉及后端、涉及数据、验收结果。后续开发时可以按这里逐项打勾。

### 阶段 0：开发前确认

目标：确认第一版边界，避免开发中途频繁改方向。

| 状态 | 工作项 | 说明 |
| --- | --- | --- |
| 已完成 | 确认默认生成参数 | 默认 `9:16`、`720p`、`5s`、动漫风格 |
| 已完成 | 确认第一版暂缓配音/字幕/音乐 | 第一版只做画面片段和视频合成 |
| 已完成 | 确认模型失败策略 | 自动切换备用模型，402 时向前端推送通知 |
| 已完成 | 确认多图合并策略 | 第一版只保留入口和任务结构 |
| 已完成 | 确认继续模拟支付 | 继续模拟支付，不阻塞主流程 |

完成标准：

- 第一版功能范围明确。
- 默认参数明确。
- 不会因为高级功能未定阻塞主流程开发。

### 阶段 1：产品骨架和底层数据

目标：先搭好全局导航、项目归属、媒体文件和任务规范，让后续功能都接在统一底座上。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| 已完成 | API 密钥页面重构 | 合并火山引擎三个 Key 为一个输入框，每个 Key 卡片显示能力标签（🎬视频/🖼图片/💬文字/🗣数字人/🔊配音）和可用模型列表（含备用模型） |
| 已完成 | 全局导航 | 用户端主导航调整为“工作台、AI 生成、短剧工作室” |
| 已完成 | 工作台 | 增加项目总览、最近任务、资产统计、算力余额、快捷入口 |
| 已完成 | 短剧首页 | 新建 `/drama` 页面，展示短剧项目列表和新建入口 |
| 已完成 | 路由 | 增加 `/generate`、`/drama`、`/drama/create`、`/drama/:id` 等基础路由 |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| 已完成 | AdminService | 重构 `API_KEY_KEYS` 定义，合并三个火山 Key 为 `volcengine_api_key`，每个 Key 标注能力类型和模型列表 |
| 已完成 | ModelConfig | 新增 `model_configs` 表，存储每个 Provider 的主/备模型关系（如 Seedance 2.0 → 1.5 降级链） |
| 未测试 | WebSocket | 新增通知网关，402 额度不足时推送通知到前端弹窗 |
| 已完成 | Workbench | 新增工作台汇总接口 |
| 已完成 | Drama | 新增短剧项目基础接口 |
| 已完成 | Media | 新增统一媒体文件接口 |
| 已完成 | Task | 梳理统一任务状态和任务来源字段 |

数据工作：

| 状态 | 数据结构 | 工作项 |
| --- | --- | --- |
| 已完成 | `model_configs` | 存储 Provider、能力类型（video/image/text）、主备模型 ID 和优先级 |
| 未测试 | `admin_notifications` | 存储 402 等系统通知记录，前端通过 WebSocket 接收 |
| 已完成 | `drama_projects` | 短剧项目主表 |
| 已完成 | `media_files` | 统一管理上传和生成文件 |
| 已完成 | `generation_tasks` | 统一图片、视频、资产、片段、合成任务 |
| 已完成 | `task_events` | 记录任务状态变化 |

验收标准：

- 用户能从导航进入三个主功能区。
- 工作台能看到基础统计和快捷入口。
- 能创建短剧项目并在短剧首页看到。
- 上传或生成的文件未来能归属到项目、资产、片段或任务。

### 阶段 2：模型能力表和提示词模板

目标：让模型、参数和提示词变成可维护配置，避免写死在页面和业务代码里。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| 已完成 | 生成表单 | 模型下拉框从模型能力接口读取 |
| 已完成 | 生成表单 | 根据模型能力动态限制比例、清晰度、时长、数量 |
| 已完成 | 后台管理 | 后台增加模型启用、禁用、参数范围配置入口 |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| 已完成 | ModelConfig | 新增模型能力配置接口 |
| 已完成 | PromptTemplate | 新增提示词模板配置和读取能力 |
| 已完成 | AIService | 调用模型前根据能力表校验参数 |
| 已完成 | AIService | 记录实际使用模型和最终提示词 |

数据工作：

| 状态 | 数据结构 | 工作项 |
| --- | --- | --- |
| 已完成 | `model_configs` | 存储模型 ID、供应商、能力、参数范围、价格 |
| 已完成 | `prompt_templates` | 存储剧本分析、资产生成、片段生成、提示词增强模板 |

验收标准：

- 页面不再硬编码模型列表。
- 不支持的参数不会出现在可选项里。
- 每次生成都能记录“原始描述”和“最终提示词”。

### 阶段 3：AI 生成中心

目标：完成普通图片/视频生成能力，作为短剧资产和片段生成的基础能力验证。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| 已完成 | `/generate` | 新建统一 AI 生成中心页面（4 Tab：文字生图片/文字生视频/图片生视频/多图合并占位）|
| 已完成 | 文字生图片 | 输入描述、选择模型、风格、数量后生成图片 |
| 已完成 | 文字生视频 | 输入描述、选择模型、比例、清晰度、时长、风格后生成视频 |
| 已完成 | 图片生视频 | 上传或选择图片，填写动作描述后生成视频 |
| 已完成 | 多图合并 | 入口+上传+任务结构，返回"功能开发中" |
| 已完成 | 生成历史 | 展示任务类型/状态/结果缩略图/失败原因/重试入口 |
| 已完成 | 快速模板 | 每个描述框上方有💡快速模板，点击填入预设中文提示词 |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| 已完成 | Generate | 新增文字生图片接口（POST /api/generate/text-to-image）|
| 已完成 | Generate | 新增文字生视频接口（POST /api/generate/text-to-video）|
| 已完成 | Generate | 新增图片生视频接口（POST /api/generate/image-to-video）|
| 已完成 | Generate | 预留多图合并接口（POST /api/generate/image-merge）|
| 已完成 | Task | 统一通过 generation_tasks 记录任务，支持重试 |
| 已完成 | Media | 生成结果自动保存到 media_files，支持本地下载 |
| 已完成 | Media | 新增 POST /api/media/upload 文件上传接口 |

验收标准：

- 用户可以完成文字生图片、文字生视频、图片生视频。
- 每次生成都有任务记录、参数记录、提示词记录。
- 失败任务可以查看原因并重试。

### 阶段 4：短剧项目和剧本大纲分析（✅ 已完成）

目标：从用户输入的大纲中自动分析出短剧结构，并允许用户编辑确认。

已完成成果：

- **2026-07-14**：完成 Phase 4 全部后端（6 实体、11 服务方法、12 控制器端点）和前端（4 页面 + 路由）开发与审计。
- **Bug 修复**：`drama.service.ts delete()` cascade 修复；`chatCompletion()` auto 模式优先级链重写（阿里云→智谱→火山引擎→OpenAI→DeepSeek）；`cleanJson()` 增加空检测；错误消息中文化；`STATUS_MAP` 补全缺失状态。
- **端到端测试通过**：创建项目 → 分析（阿里云 Qwen）→ 保存 → 确认 → 查询分集/资产，全链路正常。
- **智谱模型集成**：将 Zhipu 模型（GLM-4.5-Air / CogView-4 / CogVideoX-3）写入 `model_configs`，并在 `ai-service.util.ts` 中添加 `generateImageWithZhipu()` / `generateVideoWithZhipu()` 及对应强制供应商路由和自动模式链路。
- **构建验证**：后端 `npm run build` 通过。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| 已完成 | 短剧首页 | 展示所有短剧项目和进度 |
| 已完成 | 新建短剧 | 输入标题、大纲、风格、比例、清晰度、目标时长 |
| 已完成 | 分析结果页 | 展示题材、集数、分集、片段、资产列表 |
| 已完成 | 分析结果页 | 支持编辑集数、标题、片段、提示词、资产描述（EditAnalysis.tsx 三选项卡） |
| 已完成 | 草稿保存 | 大纲和分析结果自动保存 |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| 已完成 | Drama | 创建短剧项目 |
| 已完成 | DramaAnalyze | 调用大语言模型分析剧本大纲 |
| 已完成 | DramaAnalyze | 校验和修复模型返回 JSON（含 `cleanJson()` 空检测） |
| 已完成 | DramaAnalyze | 保存原始大纲、原始模型返回、结构化结果 |
| 已完成 | Drama | 用户确认分析结果后生成资产和分集片段草稿 |

数据工作：

| 状态 | 数据结构 | 工作项 |
| --- | --- | --- |
| 已完成 | `drama_outlines` | 保存大纲、分析状态、原始返回和结构化结果 |
| 已完成 | `drama_episodes` | 保存分集信息 |
| 已完成 | `drama_segments` | 保存片段草稿、提示词、时长、素材引用 |
| 已完成 | `drama_assets` | 保存待生成的人物、物品、场景资产 |

验收标准：

- ✅ 输入一段大纲后能生成结构化短剧方案。
- ✅ 用户能修改 AI 分析结果（信息 / 分集片段 / 资产三选项卡编辑）。
- ✅ 确认后能进入资产库并发起查询。

### 阶段 5：短剧资产库（✅ 已完成 2026-07-15）

目标：根据剧本分析结果，生成并管理人物图、物品图、场景图。

已完成成果：

- **2026-07-15**：完成 Phase 5 全部后端（5 个新端点 + 图片生成模型路由）和前端（Assets.tsx 页面 + Detail.tsx 入口 + 路由注册）开发与端到端验证。
- **Volcengine ARK Endpoint ID 改造**：完成所有旧模型 ID 到 Endpoint ID 的迁移，统一使用 `volcengine_api_key`，原 `seedance_api_key`/`seedream_api_key` 废弃。
- **E2E 测试通过**：创建项目→分析→确认→8 个资产全部通过 Seedream 4.5 生成→新增自定义资产→批量生成剩余 7 个→全链路无异常。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| 已完成 | 资产库 | 按人物、物品、场景三类展示资产（三 Tab + 栅格卡片） |
| 已完成 | 资产详情 | 展示名称、描述、提示词、状态、图片预览 |
| 已完成 | 资产生成 | 支持单个生成（⚡按钮）、批量生成（主按钮）、生成中旋转动画 |
| 已完成 | 资产编辑 | 支持编辑提示词后重新生成（`prompt()` 编辑框） |
| 已完成 | 上传替换 | 支持上传图片替换（`uploadAssetImage` 端点） |
| 已完成 | 版本选择 | 重新生成或上传替换时旧图自动移入 `candidates` 候选列表（预留字段） |
| 已完成 | Detail 入口 | 资产概览区增加「进入资产库」按钮 |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| 已完成 | DramaAsset | 获取资产列表（`GET :id/assets`） |
| 已完成 | DramaAsset | 新增（`POST :id/assets`）、编辑（`PUT assets/:assetId`）、删除（`DELETE assets/:assetId`）、锁定（`PUT` locked 字段） |
| 已完成 | DramaAsset | 单个资产生成图片（`POST :id/assets/:assetId/generate` → 自动路由到 Seedream/通义万相/CogView-4） |
| 已完成 | DramaAsset | 批量生成资产图片（`POST :id/assets/generate-all`，只处理 pending/failed 状态） |
| 已完成 | Media | 保存资产图片和候选版本（`candidates` JSON 字段） |
| 已完成 | Task | `generateAsset` 记录模型使用日志（`logModelUsage`） |

验收标准：

- ✅ 每个资产都有描述、提示词、图片和状态（8 个资产全部生成成功有图）。
- ✅ 用户可以新增资产（古代计算机）、修改资产（编辑提示词）、重新生成资产（单个/批量）。
- ✅ 短剧片段后续能引用资产主图（`drama_segments` 的 `character_refs`/`prop_refs`/`scene_refs` 引用资产 by name）。

### 阶段 6：分集视频和片段生成（已完成 2026-07-20）

目标：把每集拆成多个片段，自动绑定资产，批量生成片段视频。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| ✅ | 分集列表 Episodes.tsx | 展示每集标题、进度条、画面设置标签、每集 settings Modal |
| ✅ | 单集详情 EpisodeDetail.tsx | 展示本集所有片段 |
| ✅ | 片段编辑 | 支持修改片段提示词（Modal）、时长（3-15s 下拉框）、style/ratio/resolution（从 ep 设置读取） |
| 🔶 | 素材绑定 | 片段暂未自动绑定资产主图（需后续完成） |
| ✅ | 批量生成 | 支持单个片段生成 + 批量生成本集所有待生成片段 |
| ✅ | 片段预览 | 片段生成成功后可查看结果（自动刷新视频预览），失败显示错误提示 |
| ✅ | 生成进度 | 轮询状态显示「⏳ 生成中」，批量任务展示进度条（completed/total） |
| ✅ | 防重复提交 | submittingRef 同步拦截重复点击，API 返回前后均不会重复提交 |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| ✅ | DramaEpisode | 新增 settings 端点（PUT episodes/:episodeId/settings），支持修改 style/ratio/resolution |
| ✅ | DramaSegment | 新增智能规划端点（POST segments/:segmentId/plan），LLM 规划时长+时间线 |
| ✅ | DramaSegment | 新增 generate 端点（POST segments/:segmentId/generate），支持 width/height/style 参数 |
| ✅ | DramaSegment | 生成异步化：Bull 队列 `drama-segment` → `DramaSegmentProcessor`，前端轮询状态 |
| 🔶 | DramaSegment | 自动绑定资产主图（待实现） |
| 🔶 | Task | 控制批量任务并发和失败重试（已有队列基础，重试机制待完善） |

数据工作：

| 状态 | 数据结构 | 工作项 |
| --- | --- | --- |
| ✅ | `drama_episodes` | 新增 style/ratio/resolution 列 |
| 🔶 | `drama_segments` | 素材绑定快照、生成状态、视频结果（待补充） |
| 🔶 | `generation_tasks` | 增加短剧片段任务类型（待实现） |
| 🔶 | `media_files` | 保存片段视频（已完成基础，增强待做） |

验收标准：

- ✅ 用户能打开某一集并看到所有片段。
- ✅ 每个片段可智能规划时长，支持单独生成视频。
- ✅ 支持单个片段生成 + 批量生成全部待生成片段。
- ✅ 生成进度实时展示（⏳ 生成中），完成后自动刷新视频预览。
- ✅ 失败时显示错误提示，可重试（重新点击生成按钮）。
- ✅ 批量任务有进度条（X/Y），防止重复提交。

### 阶段 7：本集合成和成片管理（✅ 已完成）

目标：把已生成的片段按顺序合成为一集短剧视频。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| ✅ | 单集详情 EpisodeDetail.tsx | 合成前检查片段完整性（`allCompleted` 状态判断） |
| ✅ | 单集详情 EpisodeDetail.tsx | "合成本集"按钮（POST episodes/:episodeId/stitch） |
| ✅ | 成片预览 | 展示合成后视频（VideoPlayer）、合成状态标签（stitch_status） |
| 🔶 | 替换重合成 | 替换片段后重新合成入口（可复用现有按钮，未显式处理） |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| ✅ | DramaService | `stitchEpisode()` — 按片段顺序拼接视频，下载远端视频 → mergeVideos |
| ✅ | FFmpegUtil | `mergeVideos()` — FFmpeg concat demuxer 拼接 |
| ✅ | DramaEpisode | `stitch_status`（pending/stitching/completed/failed）+ `video_url` |
| ✅ | 统一参数 | mergeVideos 使用 FFmpeg concat demuxer，自动适配格式 |
| 🔶 | 生成封面图 | 可后续从视频提取封面帧 |

验收标准：

- ✅ 所有片段完成后可以合成为一集。
- ✅ 合成结果可以预览（VideoPlayer）和下载。
- 🔶 替换片段后重新合成（可手动点击合成按钮重新执行）。

### 阶段 7.5：大资产库（跨剧本共享资产库） ✅

目标：建立全局共享的大资产库，让小资产库（剧本级）与大资产库（全局级）双向互通，一次生成随处复用。

当前状态：全部功能已完成，构建验证（后端+前端 npm run build）通过。

#### 核心设计

```
                     ┌─────────────────────────┐
                     │   工作台 Dashboard       │
                     │  ┌───────────────────┐   │
                     │  │ 「大资产库」入口    │   │
                     │  └────────┬──────────┘   │
                     └──────────┼───────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                  ▼
     ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
     │ 大资产库页面   │  │  短剧工作室   │  │ AI 生成中心  │
     │ (global)      │  │ (小资产库)    │  │ (直接生成)   │
     │ 人物/场景/物品 │  │ 人物/场景/物品 │  │  → 保存到大  │
     │ 跨剧本共享     │  │ 单剧本专属    │  │    资产库    │
     └──────┬───────┘  └──────┬───────┘  └──────────────┘
            │                 │
            │    ← 导入 ←     │
            │    → 导出/保存 → │
            └─────────────────┘
```

**小资产库** = 现有 `drama_assets` 表，每个剧本独立（阶段 5 已完成）
**大资产库** = 新增 `global_assets` 表或 `drama_assets` 加 `scope=global` 标记，跨剧本共享

#### 数据结构

| 字段 | 说明 |
|------|------|
| `id` | 主键 |
| `type` | `character` / `prop` / `scene` |
| `name` | 名称 |
| `description` | 人物定位/场景作用/物品作用描述 |
| `prompt` | 图片生成提示词 |
| `image_url` | 主图 URL |
| `candidates` | 候选图列表（JSON） |
| `tags` | 标签（如 "古风/仙侠/现代"，方便筛选） |
| `source_type` | 来源：`manual`(手动创建)/`import`(从小资产库导入)/`generate`(直接生成) |
| `source_project_id` | 来源短剧项目 ID（可选） |
| `usage_count` | 被引用次数 |
| `created_at / updated_at` | 时间戳 |

#### 前端工作

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| ✅ | Dashboard | 顶部导航增加「大资产库」入口按钮（快速链接卡片） |
| ✅ | 大资产库页面 | `/global-assets` 页面已实现，展示全部人物/场景/物品资产（三 Tab + 栅格卡片） |
| ✅ | 大资产库页面 | 支持筛选（关键词搜索） |
| ✅ | 大资产库页面 | 支持直接生成图片（生成参数 Modal：风格/宽高比/画质） |
| ✅ | 大资产库页面 | 支持手动新增资产、编辑描述/提示词、上传替换图片、删除 |
| ✅ | 小资产库（Assets.tsx） | 每个资产卡片增加「保存到大资产库」按钮 |
| ✅ | 小资产库（Assets.tsx） | 增加「从大资产库导入」按钮 → 弹出大资产库选择器 Modal |
| ✅ | 大资产库选择器 Modal | 搜索/筛选大资产库，勾选后批量导入到当前小资产库 |
| ✅ | AI 生成中心 | 历史记录中已完成项增加「保存」按钮 → 选择类型/名称后保存到大资产库 |

#### 后端工作

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| ✅ | GlobalAsset | `global_assets` 实体/表已存在 |
| ✅ | GlobalAsset | CRUD 接口：列表（支持筛选/搜索/分页）、详情、新增、编辑、删除 |
| ✅ | GlobalAsset | 单图生成接口（POST `/api/global-assets/:id/generate`）增强：支持 width/height/style 参数 |
| ✅ | GlobalAsset | 统计接口：各类型数量、总引用次数 |
| ✅ | GlobalAsset | 新增智能规划（POST plan-prompt）和翻译（POST translate）端点 |
| ✅ | GlobalAsset | 控制器 `@Param('id')` 全部增加 `ParseIntPipe`，避免参数类型错误 |
| ✅ | GlobalAsset | `generateImage()` 增加 `urls` 空数组检查，防止 AI 返回空时崩溃 |
| ✅ | DramaAsset | 「保存到大资产库」接口（POST `/api/drama/assets/:id/publish-to-global`） |
| ✅ | DramaAsset | 「从大资产库导入」接口（POST `/api/drama/:id/assets/import-from-global`） |
| ✅ | 导入去重 | 同名同类型资产跳过不重复导入 |
| ✅ | 引用计数 | 导入时 `usage_count` +1 |

#### API 设计

```
# 大资产库
GET    /api/global-assets?type=character&tag=仙侠&keyword=叶辰&page=1&limit=20
GET    /api/global-assets/:id
POST   /api/global-assets                          # 新增
PUT    /api/global-assets/:id                      # 编辑
DELETE /api/global-assets/:id                      # 删除
POST   /api/global-assets/:id/generate             # 生成图片
POST   /api/global-assets/:id/upload               # 上传替换图片
GET    /api/global-assets/stats                    # 统计

# 小资产库 ↔ 大资产库
POST   /api/drama/assets/:id/publish-to-global     # 单个保存到大资产库
POST   /api/drama/:id/assets/import-from-global    # 批量从大资产库导入

# AI 生成中心保存到大资产库
POST   /api/generate/save-to-global                # 把生成结果存到大资产库
```

#### 页面布局示意

```
大资产库页面 (/global-assets)
┌─────────────────────────────────────────────────────┐
│  ← 返回工作台                                        │
│                                                     │
│  ┌─────────────────────────────────────────────────┐ │
│  │  大资产库                         [搜索...] [新增] │ │
│  │  共 42 个资产，覆盖 5 个短剧项目                  │ │
│  └─────────────────────────────────────────────────┘ │
│                                                     │
│  ┌人物(18)──┬物品(12)──┬场景(12)──┐                  │
│  │ ┌────┐ ┌────┐ ┌────┐ ┌────┐   │                  │
│  │ │叶辰│ │林清│ │玉佩│ │柴房│   │                  │
│  │ │⚡  │ │⚡  │ │⚡  │ │⚡  │   │                  │
│  │ │保存│ │保存│ │保存│ │保存│   │                  │
│  │ └────┘ └────┘ └────┘ └────┘   │                  │
│  │ 仙侠 古风                      │                  │
│  └───────────────────────────────┘                  │
│                                                     │
│  小资产库（Assets.tsx 内新增）                        │
│  ┌─────────────────────────────────────────────┐    │
│  │  人物 (5)    [从大资产库导入]  [保存到大资产库] │    │
│  │  ┌────┐ ┌────┐ ┌────┐                       │    │
│  │  │男主│ │女主│ │反派│                       │    │
│  │  │⚡  │ │⚡  │ │⚡  │                       │    │
│  │  │导出│ │导出│ │导出│                       │    │
│  │  └────┘ └────┘ └────┘                       │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

#### 验收标准

- 用户能从 Dashboard 进入大资产库页面并看到所有资产
- 用户能在小资产库点击「保存到大资产库」，资产出现在大资产库中
- 用户能在小资产库点击「从大资产库导入」，批量选择资产后导入到当前短剧
- 导入同名资产时不覆盖/覆盖可选
- 大资产库中删除资产时如已被引用给出警告
- AI 生成中心生成的结果也可以保存到大资产库（视频类型已拦截提示不支持）

### 阶段 8：工作台完善和长流程体验 🟡

目标：让整个项目可持续使用，不只是功能能跑。

已实现：WorkbenchModule（汇总/项目列表/失败任务/磁盘用量）、Dashboard 升级（项目进度卡片/继续入口/任务队列/中文失败原因/自动轮询）。

前端工作：

| 状态 | 页面/模块 | 工作项 |
| --- | --- | --- |
| ✅ | 工作台 | 展示短剧项目进度、最近任务、失败任务提醒 |
| ✅ | 工作台 | 支持从工作台继续上次制作步骤（「继续」按钮跳转） |
| ✅ | 任务面板 | 展示排队中、生成中、成功、失败任务 |
| 待做 | 全局体验 | 长流程自动保存草稿 |
| 待做 | 全局体验 | 批量任务展示预计等待时间和消耗 |

后端工作：

| 状态 | 模块 | 工作项 |
| --- | --- | --- |
| ✅ | Workbench | 汇总项目、任务、资产、算力（`/api/workbench/summary\|projects\|failed-tasks\|disk-usage`） |
| ✅ | Task | 失败原因中文化（40+ 常见错误中文映射） |
| 待做 | Credit | 生成前预估算力，成功后扣费，失败后解冻 |
| 待做 | FileCleanup | 文件清理策略和存储持久化 |

验收标准：

- 用户能清楚知道每个项目做到哪一步。
- 失败任务有明确原因和处理入口。
- 批量生成不会让用户失去进度感。

### 阶段 9：高级能力扩展

目标：在核心闭环稳定后，再逐步增强商业化和内容质量。

| 状态 | 能力 | 说明 |
| --- | --- | --- |
| 暂缓 | 多集全自动批量 | 一键生成多集，依赖前面单集流程稳定 |
| 暂缓 | 配音 | 可接 TTS 模型，为每个片段或整集生成语音 |
| 暂缓 | 字幕 | 自动生成字幕并烧录或外挂 |
| 暂缓 | 背景音乐 | 上传或生成 BGM，合成时混音 |
| 暂缓 | 片头片尾 | 短剧模板化包装 |
| 暂缓 | 多图合并增强 | 接入稳定支持多图输入的模型 |
| 暂缓 | 对象存储 | 接入 OSS/COS/R2，解决远程 URL 过期和本地空间问题 |
| 暂缓 | 真实支付 | 接入支付宝/微信支付 |
| 暂缓 | 发布平台 | 对接抖音、视频号等平台发布 |

验收标准：

- 核心短剧流程稳定后，再逐项启用高级能力。
- 每个高级能力都不破坏原来的大纲、资产、片段、合成流程。

## 15. 推荐执行顺序

如果从现在开始正式开发，建议按下面顺序推进：

> 注意：阶段 1 的第一个任务为"API 密钥页面重构 + 模型配置体系"，这是后续所有 AI 调用的基础，必须优先完成。

```text
阶段 0：开发前确认
  -> 阶段 1：产品骨架和底层数据（含 API 密钥重构 + 模型配置体系）
  -> 阶段 2：模型能力表和提示词模板
  -> 阶段 3：AI 生成中心
   -> 阶段 4：短剧项目和剧本大纲分析 ✅
   -> 阶段 5：短剧资产库 ✅
   -> 阶段 6：分集视频和片段生成 ✅
   -> 阶段 7：本集合成和成片管理 ✅
   -> 阶段 7.5：大资产库（跨剧本共享资产库）✅（全部完成 2026-07-21）
   -> 阶段 8：工作台完善和长流程体验 🟡（核心面板已完成 2026-07-21）
  -> 阶段 9：高级能力扩展 ⬜
```

当前开发已覆盖阶段 4-7 的完整闭环（阶段 7.5 全部完成，阶段 8 核心面板已完成）。第一轮开发的最小闭环：

```text
新建短剧 -> 分析大纲 -> 生成资产 -> 生成片段 -> 合成本集
```

---

## 16. API 密钥与模型管理方案

> 本方案为阶段 1 第一优先级任务，完成后所有 AI 调用均基于本方案定义的模型配置体系。

### 16.1 API 密钥页面重构设计

#### 当前问题

- 火山引擎三个输入框（Seedance、Seedream、通用）使用同一个 Key，冗余且混淆
- 每个 Key 只显示名称和描述，不显示实际能力范围和可用模型
- 用户不清楚填了 Key 之后到底能做什么

#### 新设计方案

每个 API Key 显示为一个独立卡片，包含：

```
┌──────────────────────────────────────────┐
│  🏢 火山引擎  [已配置]                    │
│  ──────────────────────────────────────   │
│  Key：••••••••25                          │
│                                           │
│  能力标签：🎬视频生成  🖼图片生成  💬文字  │
│                                           │
│  模型列表：                                │
│  ├─ 视频：Seedance 2.0 → 1.5（备用）      │
│  ├─ 图片：Seedream 4.5 → 4.0（备用）      │
│  └─ 文字：Doubao 1.5 Pro                  │
│                                           │
│  ┌ 输入新密钥（留空不修改）─────────────┐  │
│  │  [••••••••••••••••••••••••••]      │  │
│  └─────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

| Provider | 合并后 Key | 能力标签 | 主模型 → 备用模型 |
|----------|-----------|---------|-----------------|
| 火山引擎 | `volcengine_api_key` | 🎬🖼💬 | 视频：Seedance 2.0 → 1.5<br>图片：Seedream 4.5 → 4.0<br>文字：Doubao 1.5 Pro |
| 阿里云百炼 | `tongyi_api_key` | 🎬🖼💬 | 视频：通义万相视频 2.6 → 2.5<br>图片：通义万相 2.6 → 2.5<br>文字：Qwen-Plus |
| 智谱 ZAI | `zai_api_key` | 🖼🎬💬 | 文字：GLM-4.5-Air → GLM-4.7-Flash<br>图片：CogView-4-250304<br>视频：CogVideoX-3 |
| OpenAI | `openai_api_key` | 🖼💬🔊 | 图片：DALL·E 3<br>文字：GPT-4o<br>语音：TTS |
| DeepSeek | `deepseek_api_key` | 💬 | 文字：DeepSeek-Chat |
| Runway | `runway_api_key` | 🎬 | 视频：Runway Gen-3 |
| HeyGen | `heygen_api_key` | 🗣 | 数字人：唇形同步 |
| TTS | `tts_api_key` | 🔊 | 配音：备用 TTS |

#### 后端改动（admin.service.ts）

`API_KEY_KEYS` 数组简化为每个 Provider 一条：

```typescript
export const API_KEY_KEYS = [
  {
    key: 'volcengine_api_key',
    label: '火山引擎',
    capabilities: ['video', 'image', 'text'],
    models: {
      video: [{ id: 'seedance-2-0', name: 'Seedance 2.0', priority: 1 },
              { id: 'seedance-1-5', name: 'Seedance 1.5', priority: 2 }],
      image: [{ id: 'seedream-4-5', name: 'Seedream 4.5', priority: 1 },
              { id: 'seedream-4-0', name: 'Seedream 4.0', priority: 2 }],
      text:  [{ id: 'doubao-1-5-pro', name: 'Doubao 1.5 Pro', priority: 1 }],
    },
    description: '火山引擎 ARK API Key，可生成视频、图片、文字',
  },
  // ... 其他 Provider
];
```

#### 前端改动（ApiKeyManage/index.tsx）

- 读取 `API_KEY_KEYS` 定义，动态渲染卡片
- 每个卡片显示能力标签（彩色 badge）
- 模型列表用折叠面板展示，主模型粗体，备用模型灰色 + "备用"标签
- 合并后的火山引擎只显示一个输入框

### 16.2 模型调用完整流程

```
                     ┌──────────────┐
                     │  用户输入大纲  │
                     └──────┬───────┘
                            ▼
              ┌─────────────────────────┐
              │   💬 语言模型分析剧本    │
              │  火山 Doubao / 阿里 Qwen│
              │  / 智谱 GLM-5V          │
              │  输出：集数、人物、场景、 │
              │  物品、每集片段提示词     │
              └──────────┬──────────────┘
                         ▼
              ┌─────────────────────────┐
              │  🖼 图片模型生成资产     │
              │  火山 Seedream / 阿里    │
              │  通义万相                │
              │  • 人物模板（主图+备用） │ ← 保证角色一致性
              │  • 场景模板（主图+备用） │    同一角色始终用同一模板图
              │  • 物品模板（主图+备用） │
              └──────────┬──────────────┘
                         ▼
              ┌─────────────────────────┐
              │  🎬 视频模型生成片段    │
              │  火山 Seedance / 阿里    │
              │  通义万相视频 / Runway   │
              │  每段 = 场景模板图       │
              │       + 人物模板图       │
              │       + 动作提示词       │
              │  → 图片生视频，画面稳定  │
              └──────────┬──────────────┘
                         ▼
              ┌─────────────────────────┐
              │  FFmpeg 合成本集        │
              │  按片段顺序拼接          │
              │  统一分辨率/比例/帧率   │
              └─────────────────────────┘
```

**关键设计原则：**

- **角色一致性**：第一次生成人物图后锁定为主模板，后续所有该角色片段都引用此图 + 图片生视频
- **场景模板复用**：同一场景只生成一次，所有片段共享同一张场景图
- **素材快照**：每个片段保存生成时使用的素材快照，避免资产库后续改图导致历史片段无法复现

### 16.3 模型多级备用与 402 降级机制

#### 数据结构（model_configs 表）

```json
// 每个 Provider 的每个能力都有主备模型链
{
  "provider": "volcengine",
  "capability": "video",
  "models": [
    { "id": "seedance-2-0",   "name": "Seedance 2.0", "priority": 1, "status": "active" },
    { "id": "seedance-1-5",   "name": "Seedance 1.5", "priority": 2, "status": "active" },
    { "id": "seedance-x",     "name": "Seedance X",    "priority": 3, "status": "inactive" }
  ]
}
```

#### 调用逻辑

```
用户选择"火山引擎"生成视频
  → 读取 model_configs：provider=volcengine, capability=video
  → 按 priority 排序，取 status=active 的第一个
  → 调用模型 API
  → 成功 → 返回结果
  → 失败（HTTP 402 / 额度不足）：
      1. 该模型标记为 depleted（额度耗尽）
      2. 自动切换到下一个 priority 的模型
      3. 通过 WebSocket 推送通知到前端：
         "⚠️ Seedance 2.0 额度已用完，已自动切换至 Seedance 1.5"
      4. 调用日志中记录实际使用模型
  → 所有备用模型都耗尽 → 报错"火山引擎视频模型全部不可用"
```

#### WebSocket 通知实现

NestJS 内置 WebSocket 网关（`@nestjs/websockets`）：

```
后端 AIService：
  catch 402 → emit('model_depleted', { provider, model, backup })
             → 存入 admin_notifications 表
             → 推送到已连接的管理员前端

前端 admin：
  socket.on('model_depleted') → message.warning() 弹窗
```

### 16.4 模型选择下拉框

在 AI 生成中心页面的模型下拉框中，按 Provider 分组展示：

```
模型选择：[火山引擎 ▼]
          ├─ 🎬 火山引擎 (Seedance)
          ├─ 🎬 阿里云 (通义万相视频)
          ├─ 🎬 Runway (Gen-3)
          ├─ 🖼 火山引擎 (Seedream)
          ├─ 🖼 阿里云 (通义万相)
          └─ 🖼 OpenAI (DALL·E)
```

用户选择后，前端自动按该 Provider 的主模型发送请求。如果该 Provider 的模型全部耗尽，下拉框显示为红色并提示"额度已用完"。

### 16.5 涉及的文件清单

| 文件 | 改动 |
|------|------|
| `backend/.../modules/admin/admin.service.ts` | 合并火山 Key，API_KEY_KEYS 加 capabilities 和 models 字段 |
| `backend/.../modules/admin/admin.entity.ts` | 新增 `model_configs`、`admin_notifications` 实体 |
| `backend/.../utils/ai-service.util.ts` | 402 降级逻辑，模型切换，WebSocket 通知 |
| `backend/.../video/video.service.ts` | 视频生成时读取 model_configs，按用户选择调用 |
| `backend/.../websocket/notification.gateway.ts` | WebSocket 通知网关（新增） |
| `admin/.../pages/ApiKeyManage/index.tsx` | 重新设计，能力标签 + 模型列表 |
| `admin/.../pages/Generate/index.tsx` | 模型选择下拉框（后续阶段实现） |
| `admin/.../hooks/useNotification.ts` | WebSocket 通知监听（新增） |
