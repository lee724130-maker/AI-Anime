# AI-Anime 三个短板改造方案

> 目的：让项目能产出"逍遥动画"（抖音 AI 动漫短剧）同级别的成品视频。
> 现状评估：项目已覆盖制作流程的 ~90%，差距集中在 ① 配音+BGM ② 多候选择优 ③ 视觉质检。
> 本文档只做方案设计，尚未实施，供对照研究。

---

## 背景：对照目标视频的完整制作流程

| 步骤 | 逍遥视频的做法 | AI-Anime 现状 |
|------|--------------|--------------|
| 1. 剧本 | 网文式开头钩子 | ✅ 剧本 + AI 大纲分析 + JSON 结构化 |
| 2. 角色设定图 | 文生图（修罗/帝皇铠甲等） | ✅ 资产库，8 个通义万相模型 fallback |
| 3. 固定角色 | 每个镜头喂同一张角色参考图 | ✅ 参考图 I2V（4 个模型）+ R2V 多图 |
| 4. 逐镜头生成 | 批量生成大量镜头 | ⚠️ 每场景只生成 1 个，无候选 |
| 5. 挑选拼片 | 挑能用的镜头拼接 | ✅ 拼接页（多选/排序/裁剪/归一化） |
| 6. 字幕 | 烧录 | ✅ SRT 生成 + 烧录 |
| 7. 配音 | AI 旁白 | ❌ 仅 OpenAI TTS（缺 key）→ 无声片 |
| 8. BGM/音效 | 背景音乐+音效 | ❌ 无混音能力 |
| 9. 质检 | 人工盯片（他们量大） | ❌ 无自动筛查 |

---

# 短板 1：配音 + BGM（音频工程）

## 1.1 问题描述

- TTS 只接了 OpenAI（`ai-service.util.ts` 的 `generateTTS()`，约 1446-1490 行），`openai_api_key` 未配置 → 实际生成的视频**没有声音**
- FFmpeg 只能"视频+一条旁白音轨"合成（`ffmpeg.util.ts` 的 `compositeVideoWithAudio()`），**没有背景音乐混音**能力
- 后果：即便画面达标，成品也是"无声剧"，达不到发布标准

## 1.2 方案 A（首选）：阿里云 CosyVoice 接入配音 ✅ 已确认

**为什么选它**：
- 已有 `tongyi_api_key`（已验证可用），免新增供应商
- CosyVoice 是阿里云百炼的语音合成模型，中文音色质量第一梯队，支持多音色/情感
- **额度已确认（2026-08-12 用户提供）**：全部「免费额度用完即停」，各模型独立 10,000 次

**已确认模型与额度**（详见 `模型与额度统计.md`）：
| 模型 ID | 剩余/总 | 免费额度到期 | 状态 | 用途 |
|---------|---------|-------------|------|------|
| `cosyvoice-v2` | 10,000 / 10,000 | 2026-09-07 | 已开启 | **首选（男旁白 longcheng / 女声 longxiaochun）** |
| `cosyvoice-v1` | 10,000 / 10,000 | 2099-01-01 | 已开启 | 长期有效，**兜底**（音色 loongzhiqiang 男 / loongbella 女） |
| `cosyvoice-v3-flash` | 10,000 / 10,000 | 2026-09-07 | 已开启 | 快速版 |
| `cosyvoice-v3-plus` | 10,000 / 10,000 | 2026-09-07 | 已开启 | 高音质 |
| `cosyvoice-v3.5-flash` | 10,000 / 10,000 | 2026-09-07 | 已开启 | 快速版 |
| `cosyvoice-v3.5-plus` | 10,000 / 10,000 | 2026-09-07 | 已开启 | 高音质（后期升级） |
| `cosyvoice-clone-v1` | 10,000 / 10,000 | 2099-01-01 | 已开启 | 音色克隆（二期分角色，需 voice_url 参数） |

**接入方式**（已实测打通，2026-08-12 本地验证）：
```
POST https://dashscope.aliyuncs.com/api/v1/services/audio/tts/SpeechSynthesizer
   Body: { model: "cosyvoice-v2", input: { text, voice, format: "mp3", sample_rate: 48000, rate } }
   → 同步返回 output.audio.url（有效期 24h）→ 下载为 mp3（返回 ArrayBuffer）
```

**实测结论（2026-08-12 本地全链路验证）**：
- ✅ `cosyvoice-v2`：男声 `longcheng_v2` / 女声 `longxiaochun_v2`（已通过项目 `generateTTS()` 实测出 95KB/2.9s mp3）
- ✅ `cosyvoice-v3-flash`：男声 `longcheng_v3` / 女声 `longxiaochun_v3`（实测 202KB/6.3s）
- ✅ `cosyvoice-v3-plus`：男声 `longanyang` / 女声 `longanhuan`（实测 100KB/2.9s）
- ❌ `cosyvoice-v1`：**不支持 HTTP 调用**（400「current user api does not support http call」，仅 WebSocket）→ 不入降级链
- ❌ `cosyvoice-v3.5-plus/flash`：**无系统音色**（仅声音复刻/设计场景，需先创建音色）→ 不入降级链
- ⚠️ 音色名带版本后缀（v2 用 `longcheng_v2` 不是 `longcheng`；v3 用 `longcheng_v3`），跨模型混用会报错
- 当前降级链：`cosyvoice-v2 → cosyvoice-v3-flash → cosyvoice-v3-plus`；OpenAI TTS 仅作兜底

**代码改动点**：
| 位置 | 改动 |
|------|------|
| `ai-service.util.ts` `generateTTS()` | ✅ 已接入（2026-08-12）：有 `tongyi_api_key` 时优先走 CosyVoice，失败再降级 OpenAI（若有 key） |
| `video.processor.ts` Step 3（约 176-195 行） | 无需大改，`generateTTS` 返回 ArrayBuffer 的契约保持兼容 |

**音色参数建议**：旁白用"沉稳男声"（`longcheng`），女性角色用"温柔女声"（`longxiaochun`），后期可做多角色配音（不同段落指定不同音色）。

## 1.3 方案 B（备选）：智谱 TTS ❌ 已废弃

- ~~智谱 `glm-speech` 语音合成（z.ai 控制台）~~ → **用户 2026-08-12 声明智谱无法使用，本方案废弃**
- 若 CosyVoice 全部不可用，降级 OpenAI TTS（openai_api_key/tts_api_key，如已配置）

## 1.4 方案 D（配套必做）：BGM 混音 ✅ 已实现第一版

**目标**：最终视频 = 画面 + 旁白 + 背景音乐（+ 可选音效）三轨混合。

**实现**（ffmpeg `amix` 滤镜）：
- `ffmpeg.util.ts` 新增 `compositeWithMusic(video, narration, music)` 方法 ✅（2026-08-12）
- BGM 处理规则：
  - 音乐时长 > 视频 → 裁剪（`atrim`）+ 末尾淡出（`afade` 2s）
  - 音乐时长 < 视频 → 循环（`aloop`）再淡出
  - 音量平衡：旁白 1.0 / BGM 0.2（`volume`），`alimiter=limit=0.95` 防爆音
- **实测**（2026-08-12 本地）：短 BGM 2s 循环补到 8s 视频 → 输出精确 8.000s ✅；长 BGM 30s 裁到 8s ✅；无 BGM 自动回退纯旁白 ✅；淡出生效（前 4s 音量 -28dB → 末 2s -39.8dB）✅
- BGM 素材来源：管理后台「音乐库」上传/管理（参考现有资产库的写法）——**待二期**；第一版用 `system_configs.default_bgm_url` 配置（支持 `/static/` 本地或 http(s) 远程 URL），`video.processor.ts` 合成时自动读取并混音

## 1.5 成本评估

| 项 | 成本 | 备注 |
|----|------|------|
| CosyVoice 接入 | 0.5~1 天 | 轮询代码可复用 |
| BGM 混音 | 0.5 天 | 纯 ffmpeg |
| 上线后开销 | 按次计费 | 百炼免费额度内约等于 0 |

---

# 短板 2：多候选择优（"烧 tokens"机制）

## 2.1 先澄清关键概念

**生成视频不烧 tokens，烧的是"视频次数额度"。** 两者完全不同：

| 项目 | 消耗什么 | 你的额度（来自 AI-MODEL-STATUS.md） |
|------|---------|-----------------------------------|
| 剧本分析 / 提示词扩写 / LLM 规划 | tokens | qwen-plus 免费额度，开销可忽略 |
| 每生成 1 个视频候选 | 视频次数 1 次 | wan2.7-t2v 50 次 / wanx2.1-t2v-plus 180 次 / turbo 175 次 / I2V 各 50 次 |

> 结论：多候选机制消耗的是视频生成次数，与 tokens 无关，不会烧掉文字模型的额度。

## 2.2 问题描述

- 现在每个场景只生成 1 个视频任务：失败自动重试 3 次（已有），但**"成功但画面崩了"的镜头没有重生成机制**
- 目标视频（逍遥）质量好的核心原因之一：镜头有大量候选，挑能用的
- 你不需要复刻他们的"烧量"玩法，用"小候选数 + 关键镜头才多开"即可

## 2.3 方案 A（第一步）：N 选 1 候选机制

**流程设计**：
```
用户勾选"候选数 N"（默认 1，可设 2~3）
  → 同一场景生成 N 个视频任务（同一提示词，同一参考图）
  → 任务完成后前端以宫格展示 N 个候选，标 1/2/3
  → 用户勾选"采纳"1 个 → 其余标记废弃（可保留供重看）
```

**代码改动点**：
| 位置 | 改动 |
|------|------|
| `video.controller.ts` / `video.service.ts` | 生成接口增加 `candidate_count` 参数；一次创建 N 个任务，绑定同一 `scene_index` 与 `candidate_round` |
| `drama.service.ts` `executeSegmentGeneration()` | 支持循环生成 N 个候选 |
| `frontend` 视频列表/场景页 | 候选宫格展示 + "采纳/废弃"按钮 |
| 任务表 `video_tasks` | 加字段 `candidate_index`、`is_accepted`（采纳标记） |

**省次数技巧**：
- 默认 N=1，用户只对"关键镜头"开 N=2~3（打斗、特写、转场、结尾钩子）
- 候选阶段优先走 **turbo 快版模型**（便宜快），满意后可选"用好模型重生成"
- 过场/静态镜头不开候选

## 2.4 方案 B（第二步）：采纳后自动续链

- 采纳的候选自动标记为该场景的"最终素材"
- 拼接页一键"把已采纳镜头按场景顺序加入拼接列表"，跳过手动搬运
- 与现有 `stitchEpisode()`（drama.service.ts 约 946 行）衔接

## 2.5 方案 C（第三步）：半自动质检淘汰（配合短板 3）

- 生成完成后先过视觉模型初筛（明显崩的标废），人只从合格里选
- 进一步省：废片在宫格里灰显，不占人眼时间

## 2.6 成本评估

| 项 | 成本 | 备注 |
|----|------|------|
| N 选 1 候选机制 | 1~2 天 | 前后端 + 表结构 |
| 自动续链 | 0.5 天 | 与拼接页联动 |
| 额度消耗 | 关键镜头 × 2~3 次 | 180 次额度约能支撑 60~90 个关键镜头 |

---

# 短板 3：视觉质检

## 3.1 用大白话解释（问题本质）

AI 生成的视频经常"翻车"：**手变成六根指头、脸糊成一团、铠甲花纹变形、镜头闪帧**。
以前只能**人眼一个个盯**，生成 20 个镜头盯 20 遍，量大时根本盯不过来，废片就混进成片里。

**视觉质检 = 让一个"能看图的 AI"当自动质检员**：
```
生成视频 → ffmpeg 抽几帧画面 → 发给能看图的 AI（qwen3-vl-flash，阿里云百炼）
  → 问它：手正常吗？脸是设定角色吗？画面糊吗？角色和参考图一致吗？
  → 它回答：能用 / 废了
```
相当于给流水线装了一台"自动瑕疵检测机"，人只需要终审。

**成本**：看图会消耗 tokens，但用的是**阿里云百炼 qwen3-vl-flash（已实测可用：720p 单张 2042 image tokens，8 帧 ≈16k tokens，免费额度内）**。每次分析约 3000~5000 tokens，够质检几千个镜头，成本约等于零。

## 3.2 现状问题（为什么现在用不了）

- `AI-MODEL-STATUS.md` 记录：**所有 qwen-vl 视觉模型测试均返回 400**，需排查 API 调用格式 —— 该问题已于 2026-08-05 解决（本地实测 qwen3-vl-flash 成功，viral 模板分析已在用，见 `模型与额度统计.md` 第三节）
- ~~GLM-5V-Turbo（智谱 z.ai 接口）已实证可用~~ → **用户 2026-08-12 声明智谱无法使用，质检改用阿里云 qwen3-vl-flash**

## 3.3 方案 A（首选）：接入阿里云 qwen3-vl-flash

**路线**：复用项目已有视觉调用（`chatWithVision()`，viral 模板分析同链路），新增 `visualCheck()` 能力。

- 数据流：`ffmpeg 抽帧（每镜头 3~5 帧）→ base64 → qwen3-vl-flash → JSON 结论（可/废 + 原因）`
- 产物：任务完成时自动产出 `quality_report`（各镜头质量标记），前端宫格直接显示"✔ 合格 / ✘ 变形"

**代码改动点**：
| 位置 | 改动 |
|------|------|
| `ai-service.util.ts` | 新增 `visualCheck(frames, referenceImage)` 方法（复用 chatWithVision） |
| `video.processor.ts` / `drama.service.ts` | 生成完成后自动抽帧质检，结果写入任务记录 |
| `ffmpeg.util.ts` | 复用现有抽帧能力（`extractFrame`），扩展为多帧批量抽帧 |
| 管理后台 | 配置质检开关 + 质检阈值（严格/宽松） |

## 3.4 方案 B（备选）：已废弃（原为修通 qwen-vl / GLM 双保险）

- ~~排查 qwen-vl 400~~ → 已修复（3.2）；~~智谱 GLM-5V-Turbo 双保险~~ → 智谱不可用
- 兜底链：qwen3-vl-flash 失败 → qwen3.5-omni-plus（项目已有降级链）

## 3.5 方案 D（进阶）：角色一致性比对

- 生成结果与角色参考图做视觉比对（"画面中的铠甲角色与参考图是同一人/同款装甲吗？"）
- 用于淘汰"变脸"镜头——这是 AI 短剧最影响观感的问题
- 依赖方案 A 落地后，加一条比对指令即可，无需新架构

## 3.6 成本评估

| 项 | 成本 | 备注 |
|----|------|------|
| GLM-5V-Turbo 接入 | 0.5~1 天 | 调用格式已有现成参考 |
| 多帧抽帧 + 质检流程 | 0.5 天 | ffmpeg + 任务状态联动 |
| tokens 消耗 | 每镜头约 3000~5000 | 免费 2000 万额度，基本零成本 |

---

# 实施顺序与总成本

## 推荐执行顺序（按性价比）

```
第一步：短板1（CosyVoice + BGM 混音）→ 0.5~1 天 → 解决"无声片"，整体完成度提升最大
第二步：短板3（GLM-5V-Turbo 质检）   → 1~1.5 天 → 给生成结果装"自动质检机"
第三步：短板2（N 选 1 候选机制）      → 1~2 天 → 配合质检实现"生成→筛选→采纳"闭环
第四步（可选）：短板2-B（自动续链）   → 0.5 天 → 闭环流水线化
```

## 总体评估

| 项目 | 数值 |
|------|------|
| 总工作量 | 约 3~5 天 |
| 代码主要改动文件 | `ai-service.util.ts` / `ffmpeg.util.ts` / `video.processor.ts` / `drama.service.ts` + 前端 2~3 个页面 |
| 新增资金成本 | 约 0（全部走已有 key 的免费额度，CosyVoice 额度待验证） |
| 落地后能力 | 与逍遥动画同等级：有声、有 BGM、有筛选、有质检的 AI 动漫短剧流水线 |

---

# 模型与额度确认（2026-08-12 用户提供，详见 `模型与额度统计.md`）

> 结论：**三个短板全部可走阿里云百炼单供应商完成**（智谱、火山引擎用户声明已无法使用，不依赖）。
> 用户只需提供以下模型 ID 与额度，无需其他供应商。

## 语音合成（CosyVoice，用于短板 1）✅ 已开通

全部「免费额度用完即停」，各模型独立 10,000 次额度：

| 模型 ID | 剩余/总 | 免费额度到期 | 状态 |
|---------|---------|-------------|------|
| `cosyvoice-v2` | 10,000 / 10,000 | 2026-09-07 | 已开启（首选） |
| `cosyvoice-v1` | 10,000 / 10,000 | 2099-01-01 | 已开启（兜底） |
| `cosyvoice-v3-flash` | 10,000 / 10,000 | 2026-09-07 | 已开启 |
| `cosyvoice-v3-plus` | 10,000 / 10,000 | 2026-09-07 | 已开启 |
| `cosyvoice-v3.5-flash` | 10,000 / 10,000 | 2026-09-07 | 已开启 |
| `cosyvoice-v3.5-plus` | 10,000 / 10,000 | 2026-09-07 | 已开启 |
| `cosyvoice-clone-v1` | 10,000 / 10,000 | 2099-01-01 | 已开启（克隆需 voice_url） |

## 文本大模型（GLM，用于 LLM 备用链）

项目主链 qwen-plus 已在用；GLM 模型额度仍充足，保留为备用（到期 2026-09-07，`glm-5.2` 至 09-15）：

| 模型 ID | 剩余/总 | 免费额度到期 | 状态 |
|---------|---------|-------------|------|
| `glm-5` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 |
| `glm-5.1` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 |
| `glm-5.2` | 1,000,000 / 1,000,000 | 2026-09-15 | 已开启 |
| `glm-4.5` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 |
| `glm-4.5-air` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 |
| `glm-4.6` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 |
| `glm-4.7` | 1,000,000 / 1,000,000 | 2026-09-07 | 已开启 |

> 注：如果这些 GLM 模型是阿里云百炼控制台展示的（百炼已上架 GLM 系列），则可直接经 `tongyi_api_key` + compatible-mode 调用；如确认不可用，主链 qwen-plus 完全够用，GLM 仅作参考记录。

---

## 待验证事项清单

- [x] 阿里云百炼 CosyVoice 免费额度、模型 ID → **2026-08-12 已确认**（7 个模型各 10,000 次，见上表）
- [x] 智谱 glm-speech TTS → **废弃**（智谱无法使用，配音全走 CosyVoice）
- [x] qwen-vl 400 报错根因 → **已修复**（2026-08-05 本地实测通过，质检用 qwen3-vl-flash）
- [ ] `video_tasks` 表加字段的迁移方式（本地 SQLite 直接加列即可，若用 MySQL 走 synchronize）
- [ ] 生产部署前：百炼控制台复核 CosyVoice/GLM 最新额度（免费额度会随时间消耗）

> 本文档基于 2026-08-12 项目现状编写，未修改任何项目文件。

---

# 短板 2/3 实施计划表单（2026-08-12 记录）

> 短板 1（配音+BGM）已完成并实测通过；**短板 2（多候选择优）+ 短板 3（视觉质检）已于 2026-08-12 实施完成并本地验证全绿**（后端 e2e 16 项 + 前端 UI 10 项），待生产部署。
> 落地范围：短剧片段生成链路（`executeSegmentGeneration` 多候选循环 + 候选表 + 质检 + 采纳 API + EpisodeDetail 前端宫格）；生成页任务质检为二期可选。

## 短板 3：视觉质检 实施表单

| # | 步骤 | 改动位置 | 改动内容 | 验证方式 |
|---|------|---------|---------|---------|
| 3.1 | 新增 `visualCheck()` | `ai-service.util.ts`（复用 `chatWithVision` 1944 行 + qwen3-vl-flash 模型链，viral 分析同链路） | 输入 frames + 可选 referenceImage，输出 JSON 结论 `{pass: boolean, issues: string[], consistency: string}`；失败降级 `{pass: 'unknown'}` 不阻断生成 | ✅ 2026-08-12 实测：真实生成片段质检 flag/pass 均生效 |
| 3.2 | 多帧抽帧 | `ffmpeg.util.ts`（`extractFrame` 544 行已有，扩展批量） | 新增 `extractFramesAt(videoPath, times[])` 一次抽 3~5 帧，复用 viral 抽帧逻辑 | ✅ 已实现（含文件名随机后缀防碰撞） |
| 3.3 | 短剧片段自动质检 | `drama.service.ts` `executeSegmentGeneration`（586 行） | 生成完成 → 抽 3 帧 → visualCheck → 结果写 `drama_segment_candidates.quality / quality_report`（`is_accepted` 不变） | ✅ 2026-08-12 e2e：候选 0=flag（手部变形）、候选 1=pass |
| 3.4 | 生成页任务自动质检（可选二期） | `video.processor.ts` Step 4 后 | 同上，结果写 `generation_tasks.quality_report` 列（synchronize 自动建列） | ⏳ 二期 |
| 3.5 | 前端展示 | 短剧片段列表 / 生成历史 | 视频卡片角标「✔ 合格 / ✘ 变形 / ⚠ 未检」，悬停显示质检报告 | ✅ 2026-08-12 UI 实测：EpisodeDetail 宫格角标 + Tooltip 报告 10/10 |
| 3.6 | 管理后台开关（可选） | `system_configs` | `quality_check_enabled`（0/1，未配置默认开启）、`quality_check_strictness`（strict/normal/loose） | ✅ 本地已种键（enabled=1/normal）；生产部署时需同步 |

## 短板 2：多候选择优 实施表单

| # | 步骤 | 改动位置 | 改动内容 | 验证方式 |
|---|------|---------|---------|---------|
| 2.1 | 候选实体注册 | `drama.module.ts` 注册 `DramaSegmentCandidate`（实体文件已建） | 注册后 synchronize 自动建表 `drama_segment_candidates`（segment_id / candidate_index / video_url / status / quality / quality_report / is_accepted / error_msg） | ✅ 2026-08-12：表已建，e2e 写入/读取正常 |
| 2.2 | 片段多候选生成 | `drama.service.ts` `executeSegmentGeneration`（586 行）+ controller | 支持 `candidate_count`（默认 1，上限 3）；>1 时循环生成 N 个候选写候选表（同 prompt 同参考图，TTS 复用），`segment.video_url` 指向候选 0 | ✅ 2026-08-12 e2e：candidate_count=2 生成 2 条候选，扣费 240=120×2 预扣正确 |
| 2.3 | 候选列表/采纳 API | 新 `drama.controller.ts` 路由 | `GET /api/drama/segments/:id/candidates`、`POST /api/drama/candidates/:id/accept`（置 is_accepted=true + 回写 segment.video_url）、`DELETE` 弃用 | ✅ 2026-08-12 e2e：三接口 + 采纳后主视频切换 + 未登录 401 |
| 2.4 | 前端候选宫格 | 短剧 `EpisodeDetail.tsx` | 片段卡片候选数选择（1/2/3）+ 候选宫格（N 个视频缩略 + 编号 1/2/3 + ✔/✘ 质检角标 + 悬停质检报告），「采纳」按钮高亮选中；被弃候选灰显 | ✅ 2026-08-12 UI 实测：N=2 宫格、角标、采纳切换 10/10 |
| 2.5 | 采纳后自动续链（二期） | 拼接页（`stitchEpisode` 约 946 行） | 一键把已采纳候选按场景顺序加入拼接列表 | ⏳ 二期 |
| 2.6 | 省次数策略 | 前端默认值 + 提示 | 默认 N=1；打斗/特写/转场/结尾钩子才开 N=2~3；提示文案「每次候选消耗 N 次视频额度」 | ✅ 扣费联动已实现（按实际生成次数 × 定价预扣） |

## 风险与待确认

- [x] 短剧片段扣费按 `candidate_count` 翻倍：预扣 = 定价 × 候选数（`credits.service.ts` charge，2026-08-12 已验证 240=120×2）
- [x] 质检用 qwen3-vl-flash 每镜头 3~5 帧 ≈3-5k tokens，免费额度内可忽略（e2e 两次质检均成功）
- [x] 候选生成并发：N=3 时同段 3 个候选串行生成（非并发），受队列约束，无额外加锁
- [x] 质检结果仅供参考不自动删除废片（避免误删），人工终审决定采纳
- [x] 清理服务引用保护已补 `drama_segment_candidates`（否则未采纳候选视频 30 天后会被误删）
- [ ] 生产部署：后端 dist + 前端 dist + 生产 system_configs 种键（quality_check_enabled/quality_check_strictness）
