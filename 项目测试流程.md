# AI-Anime 完整测试清单（阶段 1-7）

## 准备工作

- [x] MySQL 已启动
- [x] Redis 已启动（Bull 队列需要）
- [x] 后端运行 `npm run dev`
- [ ] 前端运行 `npm run dev`
- [x] 注册一个新用户 / 有可用账号

---

## 阶段 1：基础框架（Auth / User / 基础视频 / 剧本 / 角色）

### 1.1 认证模块 — `POST /api/auth/register` + `POST /api/auth/login`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 注册新用户（username + password） | 返回 JWT token，状态码 201 | ✅ 通过测试 (201) |
| 2 | 重复用户名注册 | 返回 400/409 错误 | ✅ 通过测试 (409) |
| 3 | 注册时密码太短（<6 位） | 返回验证错误 | ✅ 通过测试 (400) |
| 4 | 使用注册的账号登录 | 返回 JWT token，状态码 200 | ✅ 通过测试 (200) |
| 5 | 使用错误密码登录 | 返回 401 | ✅ 通过测试 (401) |
| 6 | 使用不存在的用户名登录 | 返回 401 | ✅ 通过测试 (401) |
| 7 | 不带 token 访问受保护接口 | 返回 401 | ✅ 通过测试 (401) |

### 1.2 用户模块 — `GET /api/user/profile` + `GET /api/user/dashboard`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 获取用户信息 | 返回用户资料（username, credits 等） | ✅ 通过测试 (200) |
| 2 | 获取 dashboard 统计 | 返回 credits/数量/今日调用次数 | ✅ 通过测试 (200) |

### 1.3 角色模块 — `/api/character/*`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 创建角色（name + description） | 状态码 201，返回角色信息 | ✅ 通过测试 (201) |
| 2 | 获取角色列表 | 分页列表 | ✅ 通过测试 (200) |
| 3 | 获取单个角色详情 | 返回角色全部字段 | ✅ 通过测试 (200) |
| 4 | 更新角色（改名/改描述） | 返回更新后的角色 | ✅ 通过测试 (200) |
| 5 | 删除角色 | 状态码 200/204，列表不再显示 | ✅ 通过测试 (200) |
| 6 | 生成角色参考图（anime/realistic） | 调用 AI 生成，返回图片 URL | 🔶 需手动测试（需 AI Key + 前端配合） |

### 1.4 剧本模块 — `/api/script/*`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 创建剧本（title + content） | 状态码 201 | ✅ 通过测试 (201) |
| 2 | 获取剧本列表 | 分页列表 | ✅ 通过测试 (200) |
| 3 | 获取单个剧本详情 | 包含 content 和 scenes | ✅ 通过测试 (200) |
| 4 | 更新剧本 | 返回更新后数据 | ✅ 通过测试 (200) |
| 5 | 删除剧本 | 200/204，列表不再显示 | ✅ 通过测试 (200) |
| 6 | 分割剧本场景 `POST /api/script/:id/split` | content 被解析为 scenes 数组 | ✅ 通过测试 (201) |
| 7 | 更新某个场景（prompt/duration） | 场景数据更新 | ✅ 通过测试 (200) |
| 8 | 导入剧本 `POST /api/script/import` | 从 content 创建 | ✅ 通过测试 (201) |
| 9 | 导出剧本 `GET /api/script/:id/export` | 返回 JSON 文件 | ✅ 通过测试 (200) |

### 1.5 基础视频生成 — `/api/video/generate`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 创建视频生成任务（prompt + resolution/duration） | 返回 task_id，状态 pending | ✅ 通过测试 (201) |
| 2 | 查询任务状态 `GET /api/video/task/:taskId` | 返回进度/状态 | ✅ 通过测试 (200) |
| 3 | 查询视频列表 `GET /api/video/list` | 分页列表，可过滤 | ✅ 通过测试 (200) |
| 4 | 查看单个视频详情 `GET /api/video/:id` | 完整字段 | ✅ 通过测试 (200) |
| 5 | 删除视频任务 `DELETE /api/video/:id` | 200 | ✅ 通过测试 (200) |
| 6 | 批删除 `POST /api/video/batch-delete` | 多个任务同时删除 | ✅ 通过测试 (200) |
| 7 | 重试失败任务 `POST /api/video/:id/retry` | 状态重置为 pending | 🔶 需手动测试（需有失败任务） |
| 8 | 获取默认视频设置 `GET /api/video/defaults` | 默认分辨率/时长等 | ✅ 通过测试 (200) |
| 9 | 文件下载 `GET /api/video/file/:filename` | 返回视频/图片文件，支持 range | 🔶 需手动测试（需有已完成视频） |

### 1.6 FFmpeg 工具（后端日志验证）

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 启动时日志打印 | `FFmpeg binary: ...ffmpeg.exe`（npm 安装路径） | ✅ 通过测试 |
| 2 | FFprobe 二进制 | `FFprobe binary: ...ffprobe.exe` | ✅ 通过测试 |
| 3 | `output/` 目录存在 | 自动创建在 backend 下 | ✅ 通过测试 |

---

## 阶段 2：增强视频（批生成 / 拼接 / 队列 / 进度）

### 2.1 批生成视频 — `POST /api/video/batch-generate`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 传入 script + 场景列表 | 每个场景生成一个视频任务 | ✅ 通过测试 (201) |
| 2 | 所有任务都返回 task_id | 任务列表 | 🔶 需手动验证（需查数据库确认） |

### 2.2 视频拼接 — `POST /api/video/stitch`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 传入多个 video ID 拼接 | 返回合并后的视频 URL | 🔶 需手动测试（需多个已完成视频） |
| 2 | 传入空列表 | 返回 400 错误 | ✅ 通过测试 (400) |
| 3 | 传入不存在的 video ID | 返回 404 | 🔶 需手动测试 |

### 2.3 高级操作

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 批下载 `POST /api/video/batch-download` | 返回 ZIP 文件 | 🔶 需手动测试（需有视频文件） |
| 2 | 批量删除 | 所有指定任务被删除 | ✅ 通过测试 (200) |

---

## 阶段 3：短剧工作室（Drama 模块核心）

### 3.1 项目管理 — `/api/drama`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 创建项目（title + outline + genre/style/ratio 等） | 返回 project，status 'draft' | ✅ 通过测试 (201) |
| 2 | 获取项目列表 `GET /api/drama` | 分页列表，含状态标签 | ✅ 通过测试 (200) |
| 3 | 获取单个项目 `GET /api/drama/:id` | 完整信息 | ✅ 通过测试 (200) |
| 4 | 更新项目（改标题/大纲等） | 更新成功 | ✅ 通过测试 (200) |
| 5 | 删除项目（级联删除） | 项目的 episodes/segments/assets 全部删除 | ✅ 通过测试 (200) |

### 3.2 AI 分析 — `POST /api/drama/:id/analyze`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 对已有 outline 的项目发起分析 | 返回分析结果（JSON，含 episodes/segments/assets） | ✅ 通过测试 (201) |
| 2 | 获取分析结果 `GET /api/drama/:id/analysis` | 返回 DramaOutline | ✅ 通过测试 (200) |
| 3 | 保存/编辑分析结果 `PUT /api/drama/:id/analysis` | 结构化数据保存 | 🔶 需手动测试 |
| 4 | 没有 outline 时分析 | 返回 400 错误 | 🔶 需手动测试 |
| 5 | 确认分析 `POST /api/drama/:id/confirm-analysis` | 生成 episodes + segments + assets | 🔶 需手动测试（需先有分析结果） |

### 3.3 分集/分段

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 获取分集列表 `GET /api/drama/:id/episodes` | 所有分集，含进度 | 🔶 需手动测试（需有已确认分析的项目） |
| 2 | 获取分集详情 `GET /api/drama/episodes/:episodeId` | 该集的所有 segments | 🔶 需手动测试 |
| 3 | 获取 segment 列表 `GET .../episodes/:episodeId/segments` | segments 列表 | 🔶 需手动测试 |
| 4 | 更新 segment `PUT .../segments/:segmentId` | prompt/summary/duration 更新 | 🔶 需手动测试 |

### 3.4 资产（素材库）— `/api/drama/:id/assets`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 获取资产列表 `GET /api/drama/:id/assets` | 含 character/prop/scene 分类 | ✅ 通过测试 (200) |
| 2 | 添加资产 `POST /api/drama/:id/assets` | 创建新资产 | ✅ 通过测试 (201) |
| 3 | 更新资产 `PUT /api/drama/assets/:assetId` | 名称/描述/prompt/图片/状态/锁定 | ✅ 通过测试 (200) |
| 4 | 删除资产 `DELETE /api/drama/assets/:assetId` | 删除成功 | ✅ 通过测试 (200) |
| 5 | 生成单张资产图片 `POST .../assets/:assetId/generate` | 调用 AI 生成，返回 image_url | 🔶 需手动测试（需 AI Key） |
| 6 | 批量生成所有资产 `POST .../assets/generate-all` | 所有 pending 资产生成 | 🔶 需手动测试（需 AI Key） |
| 7 | 上传资产图片 `POST .../assets/:assetId/upload` | 手动指定 image_url | 🔶 需手动测试 |
| 8 | 资产锁定功能 | 锁定的资产不会在批量生成中被覆盖 | 🔶 需手动测试 |

### 3.5 分段视频生成 — `POST .../segments/:segmentId/generate`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 生成单个 segment 视频 | 构建增强 prompt → 调用 AI video → 保存 video_url | 🔶 需手动测试（需 AI Key + 资产就绪） |
| 2 | 自动绑定资产引用 | 从 segment 的 character_refs/scene_refs 查找对应资产图片 | 🔶 需手动测试 |
| 3 | 批量生成所有 segment | 遍历所有 pending 的 segment 生成 | 🔶 需手动测试 |
| 4 | 资产不够时（引用图片缺失） | 正常提示，继续使用已有资源 | 🔶 需手动测试 |

### 3.6 分集合成拼接 — `POST .../episodes/:episodeId/stitch`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 合成本集所有 segment | FFmpeg concat 合并所有视频 → 保存 video_url | 🔶 需手动测试（需 segment 视频就绪） |
| 2 | 有 segment 未完成时 | 返回错误提示 | 🔶 需手动测试 |
| 3 | 无 segment 时 | 返回错误 | 🔶 需手动测试 |
| 4 | 合成后本集状态 | stitch_status 更新为 completed | 🔶 需手动测试 |

---

## 阶段 4：管理系统

### 4.1 站点配置（无需登录）

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | `GET /api/admin/site/config` | 返回 site_name + notice | ✅ 通过测试 (200) |

### 4.2 Dashboard & 用户管理

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | `GET /api/admin/dashboard` | 总用户/脚本/API Key 数量等 | ✅ 通过测试 (200) |
| 2 | `GET /api/admin/users` | 分页用户列表 | ✅ 通过测试 (200) |
| 3 | `PUT .../users/:id/ban` | 用户被禁用/解禁 | ✅ 通过测试 (200) |
| 4 | `POST .../users/:id/recharge` | 用户积分增加 | ✅ 通过测试 (201) |
| 5 | `DELETE .../users/:id` | 用户+所有关联数据删除 | ✅ 通过测试 (200) |

### 4.3 API Key 管理

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | `GET /api/admin/api-keys` | 列出各 provider 状态（key 脱敏） | ✅ 通过测试 (200) |
| 2 | `PUT /api/admin/api-keys` | 更新/新增 API Key | ✅ 通过测试 (200) |

### 4.4 模型配置 CRUD — `/api/admin/models`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | `GET /api/admin/models` | 分页模型列表 | ✅ 通过测试 (200) |
| 2 | `GET /api/admin/models/:id` | 单个模型详情 | ✅ 通过测试 (200) |
| 3 | `POST /api/admin/models` | 创建模型（provider + capability + model_id + priority） | ✅ 通过测试 (201) |
| 4 | `PUT /api/admin/models/:id` | 更新模型参数（分辨率/时长/价格等） | ✅ 通过测试 (200) |
| 5 | `DELETE /api/admin/models/:id` | 删除模型 | ✅ 通过测试 (200) |

### 4.5 提示词模板 CRUD — `/api/admin/prompt-templates`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 列表/详情/创建/编辑/删除 | 全部正常 | ✅ 通过测试 (200/201) |
| 2 | 筛选（按 provider/capability） | 过滤结果 | ✅ 通过测试 (200) |

### 4.6 通知系统 — `/api/admin/notifications`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 列表 + 未读数 | 返回通知 | ✅ 通过测试 (200) |
| 2 | 标记已读 + 全部已读 | 状态更新 | ✅ 通过测试 (200) |
| 3 | WebSocket 实时推送 | 新通知时前端收到通知 | 🔶 需手动测试（前端配合） |

### 4.7 日志

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | `GET /api/admin/generation-logs` | 视频生成日志 | ✅ 通过测试 (200) |
| 2 | `GET /api/admin/logs` | 管理员操作日志 | ✅ 通过测试 (200) |

---

## 阶段 5：订单与积分

### 5.1 积分套餐

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | `GET /api/order/plans` | 返回套餐列表（starter/creator/studio） | ✅ 通过测试 (200) |

### 5.2 订单流程

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 创建订单 `POST /api/order/create`（plan + provider） | 返回 order_no，status pending | ✅ 通过测试 (201) |
| 2 | 查询列表 `GET /api/order/list` | 分页订单 | ✅ 通过测试 (200) |
| 3 | 模拟支付 `POST /api/order/:id/mock-pay` | 订单状态变 paid，用户积分增加 | ✅ 通过测试 (201) |
| 4 | 取消订单 `POST /api/order/:id/cancel` | 状态变 cancelled | 🔶 需手动测试 |
| 5 | 重复支付同一订单 | 返回错误 | 🔶 需手动测试 |

### 5.3 积分扣除（视频生成时）

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 生成视频时积分扣除 | 用户 credits 减少，video.credits_charged = true | 🔶 需手动测试（需真实视频生成） |
| 2 | 积分不足时 | 返回错误提示 | 🔶 需手动测试 |
| 3 | 重试任务是否重复扣积分 | 不重复扣除 | 🔶 需手动测试 |

---

## 阶段 6：媒体与任务管理

### 6.1 媒体文件 — `/api/media/*`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 上传文件 `POST /api/media/upload`（multipart） | 保存文件，返回 media 记录 | 🔶 需手动测试（需 Postman 或前端） |
| 2 | 创建媒体记录 `POST /api/media` | 手动创建 | ✅ 通过测试 (201) |
| 3 | 列表 `GET /api/media` | 可过滤（type/project_id） | ✅ 通过测试 (200) |
| 4 | 详情 `GET /api/media/:id` | 文件信息 | ✅ 通过测试 (200) |
| 5 | 删除 `DELETE /api/media/:id` | 文件+记录删除 | ✅ 通过测试 (200) |

### 6.2 任务管理 — `/api/tasks/*`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 创建任务 `POST /api/tasks` | 状态 pending，生成 event | ✅ 通过测试 (201) |
| 2 | 列表 `GET /api/tasks` | 可过滤（type/status） | ✅ 通过测试 (200) |
| 3 | 更新状态 `PUT /api/tasks/:id/status` | 更新 progress/status/error，记录 event | ✅ 通过测试 (200) |
| 4 | 事件日志 `GET /api/tasks/:id/events` | 状态变更历史 | ✅ 通过测试 (200) |

### 6.3 AI 生成直调 — `/api/generate/*`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | `POST /api/generate/text-to-image` | 调用对应 AI 返回图片 URL | ✅ 通过测试 (201) |
| 2 | `POST /api/generate/text-to-video` | 返回视频 URL | ✅ 通过测试 (201) |
| 3 | `POST /api/generate/image-to-video` | 以图生视频 | 🔶 需手动测试 |
| 4 | 生成功保存到 media 库 | media_files 表新增记录 | 🔶 需手动测试（查数据库确认） |

---

## 阶段 7：生产完善

### 7.0 HappyHorse 免费额度视频测试（新 2026-07-16）

> **前提**：后端已重启，阿里云百炼 `tongyi_api_key` 已配置，HappyHorse 模型已插入 `model_configs` 表

#### 7.0.1 文生视频（T2V）

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | Studio 选 `happyhorse-1.1-t2v` + 角色 + 剧情 + 5s | 视频生成成功，状态 completed | 🔶 待测 |
| 2 | Studio 选 `happyhorse-1.1-t2v` + 10s 时长 | 正常生成 10s 视频 | 🔶 待测 |
| 3 | Studio 选 `happyhorse-1.0-t2v` + 5s | 1.0 版本正常生成 | 🔶 待测 |
| 4 | 不选模型（自动模式） | 自动走 happyhorse-1.1-t2v 最优先 | 🔶 待测 |
| 5 | 输出视频是否有水印 | 无水印（代码已设 `watermark: false`） | 🔶 待测 |

#### 7.0.2 图生视频（I2V）

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 先文生图 → 再选 `happyhorse-1.1-i2v` 图生视频 | 基于生成的图片生成视频 | 🔶 待测 |
| 2 | 角色参考图 + I2V | 角色一致性视频 | 🔶 待测 |

#### 7.0.3 降级测试

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | HappyHorse 全部额度用完（403）后自动降级 | 自动切换到万相 wan2.7-t2v 继续生成 | 🔶 待测 |
| 2 | 所有模型都失败 | 返回明确错误 + FFmpeg 占位符兜底 | 🔶 待测 |
| 3 | 日志显示 fallback 路径 | `[MODEL_USAGE]` 日志记录每一步 | 🔶 待测 |

### 7.1 多模型自动回落（其余 Provider）

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 火山引擎 Seedance 失败 → 自动切阿里云 | 日志显示 fallback 路径 | 🔶 需手动测试 |
| 2 | 所有 AI 模型都失败 → FFmpeg 占位符 | 返回 FFmpeg 生成的彩色占位视频 | 🔶 需手动测试 |

### 7.2 编辑分析页面 `EditAnalysis.tsx`

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 查看分析结果（3 标签页） | 项目信息/分集分段/资产列表 | 🔶 需手动测试（前端配合） |
| 2 | 编辑并保存草稿 | 数据保存不触发生成 | 🔶 需手动测试 |
| 3 | 确认最终版本 | episodes/segments/assets 全部生成 | 🔶 需手动测试 |

### 7.3 WebSocket 通知（Admin）

| # | 测试项 | 预期结果 | 实际结果 |
|---|--------|---------|---------|
| 1 | 后端新通知时推送 | 前端 `/admin` namespace 收到事件 | 🔶 需手动测试（前端配合） |
| 2 | 断线重连 | WebSocket 自动重连 | 🔶 需手动测试 |

---

## 前端页面 UI 校验

| # | 页面 | 检查项 |
|---|------|--------|
| 1 | 登录/注册页 | 表单验证、错误提示、跳转逻辑 |
| 2 | Dashboard | 统计卡片、快捷入口 |
| 3 | Drama 列表 | 卡片展示、状态标签、分页 |
| 4 | Drama 创建 | 表单字段完整、提交成功跳转 |
| 5 | Drama 详情 | Steps 指示器、分析/确认按钮、进度展示 |
| 6 | 编辑分析页 | 3 标签页切换、编辑/保存/确认逻辑 |
| 7 | 资产管理页 | 3 分类 tab、生成/上传/删除/锁定交互 |
| 8 | 分集列表 | 进度条、状态展示 |
| 9 | 分集详情 | segment 卡片、生成/编辑/合成按钮、视频播放器 |
| 10 | 角色管理 | 创建/编辑/生成参考图 |
| 11 | 剧本管理 | 创建/分割场景/生成视频入口 |
| 12 | 视频管理 | 列表/过滤/批操作/预览 |
| 13 | 管理后台 | 各部分 CRUD 界面完整 |

### 新增（2026-07-17）

| # | 页面 | 检查项 |
|---|------|--------|
| 14 | 小资产库 Assets | 文字按钮布局、生成参数 Modal、3 行截断点击编辑、编辑弹框双输入框、智能规划/翻译按钮 |
| 15 | 大资产库 GlobalAssets | 与 Assets 样式一致、编辑弹框双输入框、智能规划/翻译按钮 |
| 16 | 分集详情 EpisodeDetail | 时长下拉框、智能规划按钮 |
| 17 | 编辑提示词弹框 | 中文可编辑/英文只读、智能规划/中文转英文功能正常 |
| 18 | 重复资产检测 | 同类型同名资产无法重复添加 |
| 19 | 图片水印 | 生成图片无水印 |

---

## 边界 & 异常场景

| # | 测试项 |
|---|--------|
| 1 | 网络断开时友好提示 |
| 2 | API 返回 500 时前端错误处理 |
| 3 | 大量并发请求时的表现 |
| 4 | 空数据列表显示"暂无数据" |
| 5 | 表单输入超长截断/验证 |
| 6 | 重复快速点击提交按钮（防抖/加载状态） |
| 7 | JWT token 过期后自动跳转登录 |
| 8 | 跨权限访问（普通用户访问 admin 接口） |
